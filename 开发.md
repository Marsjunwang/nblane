# Research Reader 临时开发计划

> 临时工作文档。用于把 Research Reader 的排版、批注、翻译、分析和分阶段载入问题拆成可执行开发任务；后续稳定后再合并进 `docs/zh/product/paper_reading.md` 或对应 roadmap。

> 2026-05-21 更新：FastAPI sidecar Reader 是唯一 PDF Reader 主路径；旧 Streamlit 组件启动路径已停用，仅保留事件常量契约用于兼容测试。

## 0A. 2026-05-22 Research Workspace 四个骨架 Tab 补全计划

背景：Paper Library 和 Reader 已经逐步接近真正可用的论文工作台，但 `Overview`、`Claims & Citations`、`Synthesis / Export`、`Advanced Connectors`
仍偏“数据表 + 表单 + 折叠框”骨架。它们已经有 facts 和 helper：

- `research/sources.yaml`
- `research/chunks/*.jsonl`
- `research/claims.yaml`
- `research/citations.yaml`
- `research/drafts.yaml`
- `research/connectors.yaml`
- `research/exports/`

缺口不是重新定义对象，而是把这些对象组织成可连续完成任务的交互：

```text
发现 / 导入 source
  -> 阅读 / 提取 chunk
  -> 形成 claim
  -> 绑定 citation
  -> 审阅 ready / promoted
  -> 生成 synthesis draft
  -> 带 provenance 导出到 Output / Evidence
```

### 0A.1 ReadPaper 调研结论

调研来源：

- ReadPaper 特色功能概览：<https://readpaper.com/doc/en/feature.html>
- ReadPaper AI Reader：<https://readpaper.com/doc/en/reading.html>
- ReadPaper 文献管理：<https://readpaper.com/doc/manage.html>
- ReadPaper 官网介绍页：<https://readpaper.com/features>
- ReadPaper Browser Extension：<https://readpaper.com/doc/en/browserPlugin.html>
- ReadPaper Word Plugin：<https://readpaper.com/doc/en/wordPlugin.html>

ReadPaper 的成熟交互集中在六件事：

1. **找文和导入低摩擦**：文献搜索、批量上传、浏览器插件导入、arXiv 最新文献追更。
2. **文献管理像资料库**：父子文件夹、标签、备注、阅读进度、搜索、筛选、排序、重复论文识别。
3. **Reader 直接解决理解成本**：AI 问答、论文总结、划词翻译、全文翻译、中英对照、智能解析。
4. **PDF 和笔记互相定位**：highlight、note、截图、Markdown 笔记、图表提取、参考文献定位。
5. **导出面向写作**：笔记下载、参考文献格式、Word / WPS 引文插件、多种文献格式。
6. **知识图谱和相似论文服务发现**：论文关系、相似论文、引用 / 图表 / 数据同步帮助继续探索。

对 nblane 的启发：

- `Overview` 应该像 ReadPaper 的文献工作台一样给出下一步动作，而不是静态指标。
- `Claims & Citations` 应该像 Reader 笔记和引用系统一样，从选区 / chunk / 图表 / locator 出发，形成可验证的 claim，而不是只让用户填 id。
- `Synthesis / Export` 应该服务写作和汇报，先展示来源、引用和私有风险，再导出 Markdown / BibTeX / blog candidate。
- `Advanced Connectors` 应该从配置表单升级为“发现源 + 预览 + 去重 + 导入位置 + 同步历史”的 connector inbox。

不照搬 ReadPaper 的部分：

- 不做托管型社区、私有小组同步或云端社交。
- 不优先做 Word/WPS 插件；短期先把 Markdown / BibTeX / Output Studio 闭环做好。
- 不把 AI 总结、翻译、收藏直接当 evidence。
- 不把外部 connector 的 cookie / token 写入 profile。
- 不追求一个通用文献管理 SaaS；nblane 的 Research Workspace 服务当前 goal、evidence、skill 和 public output。

### 0A.2 nblane 设计理念约束

本轮四个 tab 的补全必须继续遵守 `docs/zh/product/web-experience.md` 和 `docs/zh/product/overview.md` 的核心判断：

- **目标优先**：研究队列按 current goal、project、collection 和 pending review 排序，而不是制造信息流。
- **证据优先**：阅读、翻译、摘要只是弱信号；claim / citation / evidence candidate 才能进入更高层。
- **文件优先**：YAML / JSONL / Markdown 仍是事实源；read model 和 UI payload 是派生层。
- **候选优先**：AI 可以生成 claim、draft、export、placement suggestion；写入 evidence、blog、public output 前必须预览和确认。
- **默认私有**：source、chunk、citation、draft 默认 private；公开导出必须显示 private source risk。
- **Agent 写回可审查**：connector sync、AI claim extraction、synthesis draft 生成都要留下 run summary 或 activity item，而不是静默改长期事实。
- **Research claim 与 Evidence claim 分离**：`research/claims.yaml` 仍是 source-aware draft / ready / promoted / dismissed；进入 `evidence-pool.yaml` 时仍为 `needs_review`。

一句话定位：

> ReadPaper 解决“论文如何更快读懂和管理”；nblane Research Workspace 解决“外部资料如何变成可追溯的成长证据和公开输出候选”。

### 0A.3 当前骨架问题

#### Overview

现状：

- 有指标：papers total、reading、annotated、candidate ready、archived、connectors enabled。
- 有 review queue / safety 指标。
- 有 recent papers 表格和几个跳转按钮。

问题：

- 指标不可点击，不能直接进入队列。
- Recent papers 更像数据库表，不像工作台。
- 没有把 `source -> reader -> chunk -> claim -> citation -> draft -> export` 的漏斗可视化。
- 没有明显区分 `需要处理`、`可以继续读`、`可以审阅`、`可以输出`。
- `private_publish_risk`、`citation_broken` 等风险没有给出可操作修复入口。

#### Claims & Citations

现状：

- 三个子 tab：Chunks、Research claims、Research citations。
- 能手动创建 chunk、claim、citation。
- 能把 research claim 转成 evidence candidate。

问题：

- 交互是 CRUD 表单，不是审阅工作台。
- claim lifecycle 不明确：draft、ready、promoted、dismissed 的转换缺少按钮和守卫。
- 缺少“当前 source 的证据链”视图：chunk -> claim -> citation -> evidence candidate。
- citation quote 只做简单包含检查，没有状态化呈现。
- Reader 中生成的 claim candidates 和 durable research claims 之间还不够顺。
- 无法批量处理 ready claims、重复 claims、缺 citation claims。

#### Synthesis / Export

现状：

- 支持 citation Markdown / BibTeX 导出。
- 支持 reading note export。
- 支持从 selected claims 创建 synthesis draft。
- 支持把 synthesis draft 变成 blog candidate。

问题：

- Export 和 Synthesis 混在折叠框里，用户不清楚先后顺序。
- 导出没有 manifest：这次导出了哪些 sources / claims / citations / private risk。
- draft 只是 claims 列表拼接，缺少 outline、argument map、coverage、missing citations。
- 缺少按 goal / collection / source status / claim status 的选择器。
- Blog candidate 缺少 public readiness gate 和修复入口。

#### Advanced Connectors

现状：

- Source Inbox、Add Source、Candidate Preview、Reading Room、Connectors 都塞在 Advanced。
- Connector 可配置 arXiv / Semantic Scholar / GitHub / X / 小红书，支持 dry run 和 run now。
- secrets 已有 sanitization 和环境变量边界。

问题：

- “Advanced” 里混合了高频 source inbox 和低频 connector config。
- Connector 是配置表单，不是发现 / 预览 / 导入工作流。
- dry run 结果只显示 YAML，不像待导入队列。
- 缺少导入位置选择：Source Inbox / 当前 collection / selected collection。
- 缺少同步历史、错误解释、rate limit 状态和 dedupe reason 的人类化展示。
- X / 小红书手动导入 fallback 可以用，但 UI 还没有把 CSV / JSON / pasted links 做成明确入口。

### 0A.4 产品信息架构调整

Research 顶层 tab 保持六个，但重新定义职责：

```text
Overview
  - 工作台总览 / next actions / risk queue / ready queue

Paper Library
  - 论文资料库结构、collection、paper detail、Reader 入口

Reader
  - 单篇论文阅读、翻译、批注、AI analysis、selection -> chunk / claim / citation

Claims & Citations
  - 研究 claim 审阅台、citation 验证台、promote to evidence candidate

Synthesis / Export
  - synthesis draft、reading note pack、citation export、Output Studio candidate

Advanced Connectors
  - connector inbox、sync preview、manual import、source automation settings
```

Advanced 内部建议再拆成两层：

```text
Connector Inbox
  - Dry-run discoveries
  - Pending imports
  - Dedupe / warnings
  - Import target

Manual Capture
  - Add source
  - Paste links / CSV / JSON
  - X / 小红书 fallback

Automation Settings
  - Providers
  - Query
  - Rate limit
  - Cursor
  - Secrets boundary hints
```

### 0A.5 阶段一：先补 read model 和操作契约

目标：不要把复杂逻辑继续堆在 `pages/7_Research.py`；先抽出可测试 payload，后续 Streamlit 和 8502 都能复用。

新增 / 调整模块：

```text
src/nblane/core/research_workspace.py
  - build_research_overview_payload(profile)
  - build_research_claim_review_payload(profile, source_id="", status="", queue="")
  - build_research_export_payload(profile, scope={})
  - build_connector_inbox_payload(profile, connector_id="", dry_run_cache_id="")
  - update_research_claim_status(profile, claim_id, status, note="")
  - update_research_claim_links(profile, claim_id, chunk_refs=[], citation_refs=[])
  - verify_research_citation(profile, citation_id) -> dict
  - build_research_export_manifest(profile, citation_refs=[], claim_refs=[], source_refs=[])

src/nblane/core/research_connectors.py
  - discover_connector_items(profile, connector_id) -> preview object without write
  - import_connector_items(profile, connector_id, item_fingerprints, target={}) -> result
```

关键 payload 字段：

```yaml
overview:
  funnel_counts:
    sources: 9
    reading: 5
    chunks: 12
    claims_draft: 4
    claims_ready: 2
    citations: 8
    drafts: 1
  next_actions:
    - kind: review_claims
      label: Review 2 ready claims
      target_tab: claims
      filters:
        status: ready
  risks:
    - kind: private_publish_risk
      refs: [...]
      action: open_export_gate
  recent_activity:
    - source_id: ...
      last_read: ...
      next_action: continue_reading

claim_review:
  source:
    id: ...
    title: ...
  chunk_cards:
    - id: ...
      locator: ...
      linked_claims: [...]
  claim_cards:
    - id: ...
      status: draft
      citation_status: missing | weak | verified
      quote_status: ok | warning | missing
      promote_ready: false
  citation_cards:
    - id: ...
      claim_id: ...
      quote_check:
        ok: true
        reason: ...

export_manifest:
  sources: [...]
  claims: [...]
  citations: [...]
  private_source_refs: [...]
  unpromoted_claim_refs: [...]
  broken_citation_refs: [...]
  publish_allowed: false
```

测试：

- overview payload 统计 source / chunk / claim / citation / draft。
- claim review payload 正确标出 missing citation / broken quote / promoted claim。
- citation verification 对 exact quote、大小写、空 quote、missing chunk 给出稳定结果。
- export manifest 能列出 private source、unpromoted claim、broken citation。
- connector preview 不写 `sources.yaml` / `connectors.yaml`。

验收：

- `pages/7_Research.py` 的四个 tab 渲染逻辑变薄，只负责 UI。
- 新 payload 单测不依赖 Streamlit。
- 后续 8502 standalone 页面可以复用这些 read model。

### 0A.6 阶段二：Overview 从指标页变成 Research Command Center

目标：Overview 首屏回答“现在该处理什么”，而不是只显示总数。

布局：

```text
Research Command Center

Current goal / scope strip

Pipeline
Sources -> Reading -> Extracted -> Claims ready -> Drafts -> Exportable

Next actions
  [Continue reading] [Review ready claims] [Fix citations] [Export notes]

Risk queue
  Private publish risk
  Broken citations
  Stale translations
  Duplicate / missing PDF

Recent work
  Paper cards with next action
```

任务：

- 指标改成可点击 queue chip：
  - `Reading`
  - `Needs extraction`
  - `Claims ready`
  - `Citation broken`
  - `Private publish risk`
  - `Stale translations`
- Recent papers 从 dataframe 改成 action cards：
  - title / status / collection / last read
  - next action：Open Reader、Review claims、Export note、Fix citation。
- 增加 `Ready to review` 队列：
  - ready research claims。
  - citations with quote warning。
  - drafts with publish risk。
- 增加 `Discovery updates`：
  - connector dry-run discoveries。
  - newly imported papers。
  - duplicate risk。
- 表格保留为 `Advanced / Raw table` 折叠项。

实现范围：

- `pages/7_Research.py::_render_workspace_overview()`
- `src/nblane/core/research_workspace.py::build_research_overview_payload()`
- `web_i18n.py` 增加中英文 key。
- 可选：后续把 Overview 搬到 `8502 /research`。

验收：

- 用户打开 Overview 后无需读说明，就能看到下一步 3-5 个动作。
- 点击 `Review ready claims` 会切到 Claims tab 并带上 status filter。
- 点击 `Fix citation` 会打开对应 source / claim / citation。
- 所有风险指标都有可操作入口。
- 没有数据时显示具体空状态：导入 source、打开 Paper Library、运行 connector、继续阅读。

### 0A.7 阶段三：Claims & Citations 做成审阅工作台

目标：把手工创建表单降级为高级入口，主路径变成 claim/citation review。

主布局：

```text
Claims & Citations

Filters: Source / Status / Queue / Has citation / Visibility

┌──────────────────────┬────────────────────────────┬──────────────────────┐
│ Source evidence       │ Claim review board          │ Citation inspector    │
│ chunks / figures      │ Draft / Ready / Promoted    │ quote / locator       │
│ annotations           │ Missing citation warnings   │ source visibility     │
└──────────────────────┴────────────────────────────┴──────────────────────┘
```

Claim lifecycle：

```text
draft
  -> ready        需要 source_refs 或 chunk_refs；建议至少一个 citation
  -> promoted     只通过 Create evidence candidate / Apply candidate 后进入
  -> dismissed    可保留 rationale
```

操作：

- `Mark ready`：校验 source/chunk refs；缺 citation 时允许但 warning。
- `Request citation`：从 chunk 生成 citation 草稿。
- `Verify quote`：检查 quote 是否能在 chunk text 中定位，给出 ok/warning/missing。
- `Link chunk` / `Unlink chunk`。
- `Create citation from chunk`。
- `Promote to evidence candidate`：沿用现有 `research_claim_to_evidence_candidate()`，但在 UI 中显示 preview diff。
- `Dismiss`：要求 note 或 reason。
- `Merge duplicate claims`：后续增强，第一版只提示疑似重复。

Reader 联动：

- Reader selection 生成 chunk / claim / citation 后，Claims tab 应能直接看到新卡片。
- Claim card 显示来源：
  - `manual`
  - `reader_selection`
  - `paper_claim_extract`
  - `codex_deep_read`
- `generated_by` 和 warnings 留在 claim metadata，不进入 public output。

数据层补充：

```python
update_research_claim_status(profile, claim_id, status, rationale="")
patch_research_claim(profile, claim_id, *, text=None, type=None, confidence=None, warnings=None)
link_research_claim_chunks(profile, claim_id, chunk_ids, mode="append")
link_research_claim_citations(profile, claim_id, citation_ids, mode="append")
verify_research_citation(profile, citation_id) -> {"ok": bool, "level": "...", "message": "..."}
```

UI 分层：

- 主视图：cards + buttons。
- 手工创建 chunk / claim / citation：折叠为 `Advanced create manually`。
- YAML preview：仅在 `Show patch` 时出现。

测试：

- `Mark ready` 不允许无 source/chunk/human_note 的 claim。
- `Promote` 会生成 `needs_review` evidence candidate，不直接改 skill status。
- `Promote` 成功后 research claim 变 `promoted` 并记录 `evidence_refs`。
- `Dismiss` 保存状态但不删除 claim。
- quote verification 覆盖 ok / warning / missing chunk。

验收：

- 用户能从一个 source 的 chunks 直接审阅 claim 和 citation。
- ready / promoted / dismissed 状态一眼可见。
- 每个 promoted claim 都能追到 source/chunk/citation/evidence candidate。
- 缺引用、私有 source、断链会在卡片级别显示。

### 0A.8 阶段四：Synthesis / Export 做成写作出口

目标：导出不是“下载文本”，而是带 provenance 和安全门的写作出口。

拆成四块：

```text
Synthesis Drafts
  - 从 ready/promoted research claims 生成 outline 和正文草稿

Reading Note Pack
  - 单篇 / 多篇 source 的 chunk、claim、citation、annotation 汇总

Citation Export
  - Markdown / BibTeX 第一版；后续补 RIS / CSL JSON

Output Candidates
  - Blog draft / project update / resume bullet candidate
```

Scope selector：

- Current goal。
- Paper collection。
- Source status。
- Claim status。
- Tags / methods / datasets。
- Manual selected sources / claims / citations。

Export gate：

```yaml
publish_readiness:
  allowed: false
  blockers:
    - private source: source:research:...
    - unpromoted research claim: rclaim:...
    - broken citation quote: cite:...
  warnings:
    - citation has no exact quote
    - draft contains human note without citation
```

任务：

- `build_research_export_manifest()`：所有导出先生成 manifest。
- `save_research_export()` 写入 `research/exports/` 时同时保存 manifest：

```text
research/exports/
  20260522-claim-pack.md
  20260522-claim-pack.manifest.yaml
```

- Synthesis draft 生成时默认写：
  - title
  - claim refs
  - source refs
  - citation refs
  - outline
  - body
  - missing citation warnings
- Blog candidate preview 显示：
  - `related_sources`
  - `related_research_claims`
  - `related_citations`
  - private risk
  - unpromoted claim risk。

短期格式：

- Markdown citation list。
- BibTeX。
- Reading note Markdown。
- Blog draft candidate。

后续格式：

- CSL JSON。
- RIS。
- Word/WPS 插件不做；最多导出兼容格式。

测试：

- export manifest 正确列出 refs。
- 保存 export 同时保存 manifest。
- private source 需要用户确认才能保存 private export。
- public candidate 有 blockers 时不能直接 create blog draft，或必须显式 confirm private / draft-only。
- synthesis draft 的 source / citation refs 来自 selected claims。

验收：

- 用户能按 collection / goal 选择一组论文，生成 reading note pack。
- 用户能从 promoted claims 生成 blog draft，并看到所有来源和风险。
- 导出文件旁边有 manifest，可回溯当时使用了哪些 source / claim / citation。
- public-facing 输出不会静默包含 private source。

### 0A.9 阶段五：Advanced Connectors 做成 Connector Inbox

目标：把 connector 从“配置并运行”改成“发现候选 -> 预览 -> 去重 -> 选择导入目标 -> 写入 Source Inbox / Library”。

主布局：

```text
Advanced Connectors

Connector Inbox
  Provider cards: arXiv / Semantic Scholar / GitHub / X / Xiaohongshu
  Last run / next run / status / imported / skipped / error

Discoveries
  Candidate cards
  Duplicate reason
  Import target
  Import selected

Manual imports
  Paste URLs
  CSV / JSON rows
  X / 小红书 fallback

Automation settings
  Query / limit / privacy / rate limit / cursor
```

Provider rules：

- arXiv / Semantic Scholar / GitHub：官方 API 自动发现。
- X/Twitter：优先 official API bearer env；无 token 时只显示 manual import。
- 小红书：只做 manual import，不引导 cookie scraping。
- 所有 token / cookie / secret 只读 env 或 session，不写 `research/connectors.yaml`。

Connector preview 对象：

```yaml
preview:
  connector_id: arxiv:...
  provider: arxiv
  discovered: 10
  candidates:
    - fingerprint: ...
      title: ...
      url: ...
      duplicate:
        is_duplicate: true
        reason: same_url | same_external_id | title_hash
        existing_source_id: ...
      import_target:
        kind: source_inbox | collection
        node_id: ...
      selected: false
```

任务：

- dry run 结果保存到 session 或临时 cache，不写 source facts。
- UI 用 candidate cards 替代 YAML dump。
- 支持导入目标：
  - Source Inbox。
  - 当前 Paper Library collection。
  - 选择 collection。
  - metadata only。
- 导入成功后给出 next action：
  - Open in Paper Library。
  - Run extraction。
  - Open Reader。
  - Review imported sources。
- connector run summary 写入 `last_result` 和可选 Agent Activity。
- manual import 支持：
  - pasted URLs。
  - CSV：title,url,kind,authors,tags,summary。
  - JSON list。

测试：

- dry run 不写文件。
- import selected 只写 selected candidates。
- duplicate candidates 默认 skipped。
- secrets 继续不会出现在 connector config 或 sources。
- collection target 写入 `library_node_refs`。
- manual CSV / JSON import normalization。

验收：

- 用户不看 YAML 也能知道 connector 找到了什么。
- 用户能在导入前排除重复项和无关项。
- 用户能把 arXiv 搜到的论文直接导入当前 collection。
- X / 小红书无 token 时仍有清晰手动导入路径。

### 0A.10 阶段六：8502 高交互迁移边界

短期仍可以在 Streamlit 完成四个 tab 的主要升级；但高频交互应逐步迁到 FastAPI + React：

优先迁移到 8502 的部分：

- Claim review board。
- Citation inspector。
- Connector discovery candidate cards。
- Export manifest preview。

保留在 8503 的部分：

- 低频配置。
- AI config popover。
- Raw YAML preview。
- fallback 表单。

迁移原则：

- 先抽 core payload / event handler，再迁 UI。
- 8502 和 8503 共享同一套 read model。
- 不新增数据库；仍读写 profile 文件。
- 事件 payload 不允许传任意文件路径。

### 0A.11 推荐排期

Week 1：Read model + Overview

- 新增 overview / claim review / export manifest payload。
- Overview 改成 action-oriented command center。
- 单测覆盖统计、风险、next actions。

Week 2：Claims & Citations

- Claim cards、status filters、citation inspector。
- claim lifecycle buttons。
- quote verification。
- promote to evidence candidate preview。

Week 3：Synthesis / Export

- scope selector。
- export manifest。
- reading note pack。
- synthesis draft outline。
- blog candidate readiness gate。

Week 4：Connector Inbox

- provider cards。
- dry-run candidate cards。
- import selected。
- manual CSV / JSON / pasted URL fallback。
- run history and error display。

Week 5+：8502 React polish

- claim review board standalone。
- connector discovery standalone。
- export preview standalone。
- Playwright screenshots and interaction checks。

### 0A.12 最小验收剧本

用一篇 arXiv 论文和一个 GitHub repo 完成：

1. Advanced Connectors 中运行 arXiv dry run。
2. 在 candidate cards 中选择一篇论文，导入到当前 collection。
3. Overview 出现 `Continue reading` 和 `Needs extraction` action。
4. Paper Library 打开论文，Reader 提取 segments / 创建 chunk。
5. Reader selection 创建 claim 和 citation。
6. Claims & Citations 中看到 claim card，quote verification 为 ok。
7. 点击 `Mark ready`，再点击 `Create evidence candidate`。
8. Synthesis / Export 中按当前 collection 生成 reading note pack。
9. 从 ready/promoted claims 生成 synthesis draft。
10. 生成 blog candidate 时看到 `related_sources`、`related_research_claims`、`related_citations`。
11. 若 source 为 private，export gate 阻止直接 public use，并给出修复入口。

最终产品标准：

> 用户从外部发现资料到形成可输出草稿，全程都能看见 source、chunk、claim、citation 和 private risk，而不是在几个表单之间猜下一步。

## 0. 2026-05-21 Paper Library 工作台改造记录

本轮已把 `pages/7_Research.py` 的 Paper Library 从数据库表格式页面升级为 Streamlit 版三栏工作台：

- 左侧常驻 Smart Views 和 Collections，支持 All Papers、Unsorted、Reading、PDF Missing、Needs Extraction、Claims Need Review、Duplicate Risk、Stale Translation、Recently Read、Private、Reviewed、Archived、Discarded 等队列。
- 中间区域改为可搜索、可排序、可批量选择的 paper card 列表，并保留高级 table view。
- 右侧详情面板显示论文标题、作者/年份/路径、PDF/segment/annotation/claim/citation 指标和快速动作。
- 快速动作已包含 Open Reader、同步到 Reader tab、Run extraction、Auto chunk、状态/节点/标签整理。
- `src/nblane/core/research_papers.py::paper_rows()` 已补充 workbench 需要的展示字段和视图筛选，新增测试覆盖 no-PDF、needs-extraction、duplicate-risk 与节点组合筛选。

本轮未完成，后续继续：

- **可拖拽 Collections Tree**：当前仍是 Streamlit 按钮树，不能像 blog outline 一样拖拽排序、拖入论文、右键重命名/移动/删除。
- **论文列表虚拟滚动和键盘操作**：paper card 已可用，但大量论文时仍依赖 Streamlit 全量渲染；需要自定义 component 支持虚拟列表、方向键选择、多选范围选择。
- **真正的详情抽屉**：右侧面板已常驻，但不是可收起/浮动 drawer；移动端和窄屏需要单独交互设计。
- **Reader 内联闭环**：Library 可以打开 Reader，但还没有从 Library 直接预览 PDF 页、选区创建 note/claim/citation，完整闭环仍在 sidecar Reader 内。
- **搜索 query grammar**：目前是关键词搜索；后续支持 `tag:robotics status:reading has:pdf claims:0 after:2026-05-01` 等结构化查询。
- **Duplicate merge 工作流**：现在只展示 duplicate-risk 队列；还缺少 compare metadata、选择主记录、合并 tags/notes/assets/artifacts 的安全流程。
- **Import wizard**：Paper Search 导入已有 library location，但还没有统一的 PDF / URL / BibTeX / arXiv 多入口导入向导。
- **Saved filters**：Smart Views 是系统预设；后续需要用户保存自定义筛选，并同步到 profile 级配置。
- **批量后台任务状态**：Bulk action 已能 move/status/tag，但批量 GROBID、批量 extraction、批量 claim generation 还需要 task queue 和进度反馈。
- **视觉 polish 和响应式验证**：当前是 Streamlit CSS 增强版；后续要用 Playwright/截图检查宽屏、窄屏、长标题、长 badge、中文标题和无 PDF 状态。

### 0.1 Paper Library Tree-first 和 Manage Collections 改造计划

当前 Paper Library 的 Smart Views 很适合处理系统任务，例如 PDF missing、Needs extraction、Duplicate risk，但它不适合作为长期论文资料库的主结构。用户真正需要记住的是论文在研究地图中的位置：属于哪个 project、topic、method、dataset、benchmark 或问题簇。因此后续改造目标是把左侧从“状态筛选列表”升级成“稳定的研究资料库树”，同时保留 Smart Views 作为工作队列。

核心原则：

- **Collections / Library Tree 是认知结构**：回答“这篇论文应该放在哪里”。
- **Smart Views / Work Queue 是处理结构**：回答“下一步要处理哪些论文”。
- **Technical Taxonomy 是模型辅助结构**：由模型 meta 抽取生成 topic、method、dataset、benchmark 等维度，但不能静默覆盖用户 collection。
- **Graph Cluster 是未来高级视图**：当 relation edges 足够多后，用图聚类表达论文之间的真实研究关系。
- **树是 virtual hierarchy，不是真实文件夹**：移动论文只改 `source.library_node_refs`，不移动 PDF、annotation、claim、citation 或 reader artifacts。

2026-05-22 追加判断：

- **Paper Search 应收进 Paper Library**：它不是一个独立资料库，而是 Library 的发现 / 导入工作流。用户应在某个 collection 或 smart view 语境中搜索论文，先看到标题、作者、年份、摘要、相关性理由、PDF 状态和重复风险，再选择导入位置。
- **搜索结果默认预览，不直接入库**：候选论文只有在用户确认后才写入 `research/sources.yaml`，并写入目标 `library_node_refs`。
- **导入后回到资料库视角**：成功导入后应自动聚焦到目标 collection，并选中刚导入的论文，形成 `搜索 -> 看摘要 -> 加入 Paper Library -> 继续阅读 / 整理` 的闭环。
- **顶层导航不需要 Paper Search tab**：Research 顶层更适合保留 `Overview / Paper Library / Reader / Claims & Citations / Synthesis / Export / Advanced Connectors`。搜索入口作为 Paper Library 顶部的 `Find and import papers` 区域。

目标左侧结构：

```text
Library
  All Papers
  Unsorted Inbox
  Recently Read

Collections
  All collections
  <user collection tree>

Technical Taxonomy
  Topics
  Methods
  Datasets
  Benchmarks

Work Queue
  Reading
  PDF Missing
  Needs Extraction
  Claims Need Review
  Duplicate Risk
  Stale Translation

System
  Private Sources
  Reviewed
  Archived
  Discarded
```

#### 0.1.1 阶段一：重排左侧信息架构

先在现有 Streamlit 页面里完成信息架构重排，不等自定义组件。

改动范围：

- `pages/7_Research.py::_render_paper_library()`
- `pages/7_Research.py::_LIBRARY_VIEW_LABELS`
- `pages/7_Research.py::_paper_tree_buttons()`

任务：

- 把当前平铺的 Smart Views 拆成 `Library / Work Queue / System` 三组。
- 把 Collections 上移到 Work Queue 之前，作为主要资料库入口。
- 将 `Smart Views` 文案改为 `Work Queue` 或分组内标题。
- 保留当前所有 view id 和 `paper_rows(..., view=...)` 筛选逻辑，避免破坏现有行为。
- 当前节点选中后，中间列表 caption 显示 collection path，例如 `VLA / Memory`。

验收：

- 用户第一眼看到的是 `Library` 和 `Collections`，不是 PDF missing 等维护状态。
- `PDF Missing`、`Needs Extraction`、`Duplicate Risk` 等仍可点击，并显示正确数量。
- `All Papers`、`Unsorted Inbox` 和 collection 过滤行为保持兼容。

#### 0.1.2 阶段二：补齐 Paper Library Tree 后端操作

现有数据层已有 `PaperLibraryNode`、`PaperLibraryTree`、`upsert_paper_library_node()` 和 `move_papers_to_node()`，但 Manage Collections 需要完整的文件树操作 API。

改动范围：

- `src/nblane/core/research_papers.py`
- `tests/test_research_papers.py`
- `profiles/template/research/library-tree.yaml`

新增或补齐函数：

```python
create_paper_library_node(profile, title, parent_id="", description="", color="", icon="")
rename_paper_library_node(profile, node_id, title)
position_paper_library_node(profile, node_id, parent_id="", before_node_id="", after_node_id="")
reorder_paper_library_node(profile, node_id, direction)
trash_paper_library_node(profile, node_id, paper_policy="move_to_parent")
restore_paper_library_node(profile, node_id)
purge_paper_library_node(profile, node_id, paper_policy="move_to_unsorted")
remove_papers_from_node(profile, source_ids, node_id)
set_paper_primary_node(profile, source_id, node_id)
```

规则：

- 不能移动 root。
- 不能把节点移动到自己或自己的后代下面。
- `position_paper_library_node()` 支持 before / after / into，一次保存完成 parent 和 order 更新。
- sibling order 统一重排为 `10, 20, 30...`，避免长期拖拽后 order 混乱。
- 删除 collection 前必须选择论文处理策略：
  - 移动到父 collection。
  - 移动到 Unsorted Inbox。
  - 移动到指定 collection。
  - 取消删除。
- 删除 collection 不删除 source、PDF、annotations、segments、claims、citations。
- `library_node_refs[0]` 暂定为 primary location，后续如有需要再升级为 `library_links`。

可参考实现：

- `src/nblane/core/public_site.py::position_public_library_node()`
- `src/nblane/core/public_site.py::reorder_public_library_node()`

单元测试：

- create / rename / move / reorder / trash / purge node。
- reject self-parent 和 descendant-parent。
- 删除带论文的 collection 时，source refs 按策略迁移。
- `move_papers_to_node(..., append=False)` 替换 primary location。
- `move_papers_to_node(..., append=True)` 增加 secondary location。
- `paper_rows(profile, node_id=...)` 在移动节点和移动论文后仍正确。

#### 0.1.3 阶段三：实现真正的 Paper Library Tree 组件

纯 Streamlit button tree 无法自然支持右键菜单、拖拽排序、拖入论文、多选投递和键盘操作。需要新增一个 Paper Library 专用 Streamlit custom component，或把现有 blog library tree 组件抽象后复用。

新增目录建议：

```text
src/nblane/paper_library_component/
  __init__.py
  frontend/
    package.json
    vite.config.js
    src/
      main.jsx
      PaperLibraryTree.jsx
      PaperLibraryRow.jsx
      PaperLibraryContextMenu.jsx
      PaperLibraryDialog.jsx
      MoveTargetPicker.jsx
      dndHelpers.js
      payload.js
      style.css
```

可复用参考：

- `src/nblane/public_blog_editor_component/frontend/src/library/LibraryRow.jsx`
- `src/nblane/public_blog_editor_component/frontend/src/library/LibraryContextMenu.jsx`
- `src/nblane/public_blog_editor_component/frontend/src/library/dndHelpers.js`

组件输入 payload 示例：

```json
{
  "active_view": "all",
  "active_node_id": "paper-node:vla-memory",
  "selected_paper_ids": ["source:paper:a", "source:paper:b"],
  "sections": [
    {
      "id": "library",
      "title": "Library",
      "items": [
        {"id": "all", "type": "view", "title": "All Papers", "count": 8},
        {"id": "unsorted", "type": "view", "title": "Unsorted Inbox", "count": 2}
      ]
    },
    {
      "id": "collections",
      "title": "Collections",
      "items": [
        {
          "id": "paper-node:vla",
          "type": "collection",
          "title": "VLA",
          "count": 12,
          "children": [
            {"id": "paper-node:vla-memory", "type": "collection", "title": "Memory", "count": 4}
          ]
        }
      ]
    },
    {
      "id": "work_queue",
      "title": "Work Queue",
      "items": [
        {"id": "no_pdf", "type": "view", "title": "PDF Missing", "count": 4},
        {"id": "needs_extraction", "type": "view", "title": "Needs Extraction", "count": 4}
      ]
    }
  ],
  "capabilities": {
    "create_collection": true,
    "rename_collection": true,
    "move_collection": true,
    "delete_collection": true,
    "drop_papers": true
  }
}
```

组件返回事件：

```json
{
  "action": "paper_library_create_collection",
  "event_id": "20260521-...",
  "payload": {
    "title": "Memory",
    "parent_id": "paper-node:vla"
  }
}
```

事件清单：

```text
paper_library_select_view
paper_library_select_collection
paper_library_create_collection
paper_library_rename_collection
paper_library_move_collection
paper_library_reorder_collection
paper_library_trash_collection
paper_library_purge_collection
paper_library_drop_papers_to_collection
paper_library_add_selected_papers_to_collection
paper_library_move_selected_papers_to_collection
paper_library_remove_papers_from_collection
paper_library_import_paper_here
paper_library_suggest_subtopics
paper_library_suggest_locations
```

验收：

- collection 节点可展开 / 折叠。
- 支持搜索 collection。
- 支持右键菜单。
- 支持节点拖拽排序和拖拽到其他节点下。
- 支持选中论文后右键 collection 添加或移动。
- 原 `Manage collections` expander 不再是主要入口。

#### 0.1.4 阶段四：Manage Collections 人类化

现在的 `Manage collections` 表单要求用户手动填 node title、node id、parent id，不符合论文管理工具习惯。后续应改成文件管理器式交互。

Collections 根部右键菜单：

```text
New collection
Paste papers here
Expand all
Collapse all
Validate library
Export tree
```

普通 collection 右键菜单：

```text
New subcollection
Import paper here
Add selected papers here
Move selected papers here
Rename
Move collection...
Delete collection...
Suggest subtopics
Suggest papers for this collection
```

论文卡片右键菜单：

```text
Open Reader
Show details
Move to collection...
Add to collection...
Remove from current collection
Mark as reading
Archive
Discard
Run extraction
```

左侧顶部快捷入口：

```text
Collections    [+] [...]
```

- `+`：在当前选中 collection 下创建子 collection；未选中时创建顶层 collection。
- `...`：展开更多管理动作，例如 Expand all、Collapse all、Sort A-Z、Show empty collections、Validate library。

批量整理体验：

```text
3 papers selected
Drag them into a collection
or right-click a collection -> Add selected papers here
```

验收：

- 用户可以不打开任何隐藏表单完成创建、重命名、移动、删除 collection。
- 多选论文后可通过右键 collection 完成添加或移动。
- 删除 collection 有确认弹窗，并要求选择 paper policy。
- 每个操作都有 toast / notice 反馈。

#### 0.1.5 阶段五：论文拖拽到 Collection

分两步做，避免一次性把中间列表全部 React 化导致改动过大。

Phase 5A：先支持多选论文投递。

- 保留现有 Streamlit paper card。
- 中间列表继续用 `st.multiselect` 批量选择论文。
- 左侧 collection 右键菜单新增 `Add selected papers here` 和 `Move selected papers here`。
- 后端调用 `move_papers_to_node()` 或后续新增的 append/remove API。

Phase 5B：把 paper list 也纳入 React component。

- paper card 支持真实拖拽。
- 支持单篇拖拽到 collection。
- 支持多选后一起拖拽。
- drop 时左侧目标 collection 高亮。
- drop 后中间列表、左侧 count、右侧 detail panel 一起刷新。

拖拽规则：

- paper -> collection：默认移动 primary location。
- paper + modifier -> collection：增加 secondary location。
- selected papers -> collection：批量移动或批量添加。
- paper -> Work Queue：不允许，因为 Work Queue 不是存储结构。
- collection -> Work Queue：不允许。
- collection -> collection center：移动为子节点。
- collection -> collection top/bottom：同级 before / after 排序。
- technical generated node 默认不可作为真实存储目标，除非用户选择 Promote to collection。

验收：

- 5A 完成后，用户能通过多选 + 右键 collection 完成批量归档。
- 5B 完成后，用户能像文件管理器一样拖拽论文卡片到左侧 collection。

#### 0.1.6 阶段六：导入论文时直接选择 Collection

Paper Search 导入不应该只进入全局 inbox。用户在某个 collection 下搜索时，默认导入位置应该是当前 collection。

改动范围：

- `pages/7_Research.py` 的 Paper Search 导入表单。
- `src/nblane/core/research_papers.py` 的 import / search result handling。

导入流程：

```text
Paper Search
  -> Select papers
  -> Choose library location
  -> Optional: create child collection
  -> Optional: accept AI placement suggestions
  -> Import into Paper Library
  -> Open Reader or Stay in Library
```

导入选项：

- Import to current collection。
- Choose existing collection。
- Create new child collection。
- Ask AI to suggest collection。
- Import to Unsorted Inbox。
- Import as metadata only。
- Download open-access PDF。
- Upload local PDF。

规则：

- 在 collection 中打开 search 时，默认 `library_node_refs=[current_node]`。
- AI suggestion 只能作为 preview，用户确认后才写入。
- 导入后可以选择留在 Library 或打开 Reader。

验收：

- 从 `VLA / Memory` 下搜索并导入，论文默认出现在该 collection。
- 导入时可以创建 `VLA / Memory / Episodic Memory`。
- AI suggested location 显示 confidence 和 rationale，但不会静默移动论文。

#### 0.1.7 阶段七：模型 technical meta 抽取和自动分类

用户 Collections 解决“我为什么收藏它”；模型 technical meta 解决“论文客观上属于哪些技术维度”。两者必须并存。

建议结构：

```yaml
technical_meta:
  domains:
    - Embodied AI
    - Robotics
  tasks:
    - Robotic Manipulation
  methods:
    - Diffusion Policy
    - Vision-Language-Action
  datasets:
    - CALVIN
    - BridgeData
  benchmarks:
    - Multiview 3D Consistency
  modalities:
    - vision
    - language
    - action
  contribution_type:
    - dataset
    - method
  confidence:
    methods:
      Diffusion Policy: 0.86
  evidence:
    methods:
      Diffusion Policy:
        source: abstract
        quote_ref: segment:...
  reviewed:
    methods: false
```

任务：

- 增加单篇 `Extract technical meta`。
- 增加批量 technical meta extraction。
- 左侧生成虚拟 `Technical Taxonomy`：
  - Topics
  - Methods
  - Datasets
  - Benchmarks
- 支持用户确认 / 修正模型分类。
- 支持将某个 technical node `Promote to collection`。

验收：

- 模型分类有 confidence 和 evidence，不是黑盒 tag。
- 用户 collection 不会被模型静默改写。
- technical taxonomy 可以用于浏览，但默认不承担“真实归档位置”。

#### 0.1.8 阶段八：Graph Cluster / Library Map

当 technical meta、claims、citations、chunks 足够多后，新增图聚类视图，而不是继续堆属性分类。

关系模型：

```text
Paper -> belongs_to -> Topic
Paper -> proposes -> Method
Paper -> evaluates_on -> Dataset
Paper -> improves_over -> Baseline
Paper -> supports -> Claim
Paper -> cites -> Paper
Paper -> shares_method_with -> Paper
```

目标视图：

- Cluster View：按相似论文聚类。
- Method Lineage：方法演进线。
- Dataset / Benchmark Map：数据集和评测关系。
- Claim Network：论文观点、证据和引用关系。
- Citation Neighborhood：当前论文的上下游引用邻域。

规则：

- Graph Cluster 是探索视图，不替代左侧 collection tree。
- 每个 cluster 必须能解释“为什么这些论文在一起”。
- 用户可以把 cluster 保存为 collection。

验收：

- 从 collection 进入 graph view。
- 从单篇 paper 查看 related papers。
- cluster 可保存为 user collection。

#### 0.1.9 阶段九：危险区：单篇论文真实删除

当前 `Discard` 只是把 paper source 的 `status` 改成 `discarded`，用于从阅读队列中降噪；
它不是删除。真正删除必须作为独立危险动作开发，避免误删 PDF、批注、chunks、claims
或 citations。

2026-05-22 执行状态：

- 已完成后端真实删除链路：preview、delete source record、可选删除 PDF asset、可选删除 reader artifacts、purge discarded papers。
- 已完成 8502 Paper Library `/events` 接入：删除前返回 `delete_preview`，删除后刷新 payload。
- 已完成 8502 standalone React Danger Zone：单篇论文详情底部提供 `Delete paper...`，必须先拿到 preview 且输入 source id 才能确认。
- 已完成安全阻断：active annotations、chunks、claims、citations、evidence refs 会阻止 source record 删除；PDF asset 仍需校验受控 asset root 和共享引用。
- 已完成测试覆盖：核心删除函数、8502 API preview/delete、阻塞删除、前端 payload 测试和 production build。

目标：

- 明确区分 `Mark as discarded` 和 `Delete paper...`。
- 默认提供可恢复/可预览的安全路径，不把永久删除藏在普通右键菜单里。
- 允许用户清理错误导入、重复导入、测试 PDF 和明确不再保留的 discarded papers。
- 所有危险动作必须先展示影响范围，再要求二次确认。

入口设计：

- 论文卡片右键菜单中把 `Discard` 改名为 `Mark as discarded`。
- 论文详情面板底部新增 `Danger zone`。
- `Danger zone` 内提供：
  - `Delete paper record...`
  - `Delete PDF asset...`
  - `Delete extracted artifacts...`
  - `Purge discarded papers...`（仅在 Discarded smart view 或批量选择时出现）
- 点击危险动作后打开确认弹窗，必须输入 source id 或论文标题片段。

删除对象边界：

| 对象 | 位置 | 默认行为 |
| --- | --- | --- |
| source record | `research/sources.yaml` | `Delete paper record` 删除，All Papers 不再显示。 |
| library refs | source 内 `library_node_refs` | 随 source record 删除自然消失。 |
| PDF asset | `research-assets/profiles/<profile>/papers/...` | 默认保留；勾选 `delete_pdf_asset` 才删除。 |
| pages / segments / translations / analysis | `research/paper-*`、`research/analysis` | 默认保留或随 `delete_artifacts` 删除，需预览。 |
| annotations / chunks / claims / citations / evidence refs | profile research/evidence 数据 | 默认阻止删除；需要用户先解除引用，或使用高级 `unlink_refs`。 |

后端新增函数建议：

```python
build_paper_delete_preview(profile, source_ids) -> dict
delete_paper_record(
    profile,
    source_ids,
    *,
    delete_pdf_asset=False,
    delete_reader_artifacts=False,
    unlink_refs=False,
) -> dict
delete_paper_pdf_asset(profile, source_id) -> dict
delete_paper_reader_artifacts(profile, source_id) -> dict
purge_discarded_papers(profile, *, delete_pdf_asset=False, delete_reader_artifacts=False) -> dict
```

事件清单：

```text
paper_library_delete_paper_preview
paper_library_delete_paper_record
paper_library_delete_paper_asset
paper_library_delete_paper_artifacts
paper_library_purge_discarded_papers
```

安全规则：

- 删除前先返回 preview：将删除的 source、PDF、artifacts、受影响 refs、阻塞项、warnings。
- 只要存在 active annotations、chunks、claims、citations 或 evidence refs，默认拒绝删除 source record。
- PDF asset 删除必须校验 asset ref 在受控 research asset root 内，且没有被其他 source 共享引用。
- `purge_discarded_papers` 只处理 `status=discarded` 的 paper，且先展示批量 preview。
- 所有删除都走现有 git backup / file snapshot guard，不绕过冲突检查。
- API 不接受任意文件路径，只接受 source id 和布尔策略。

验收：

- `Discard / Mark as discarded` 后 paper 仍在 All Papers，但状态显示为 Discarded。
- `Delete paper record` 后 paper 从 All Papers、collection filter、detail payload 中消失。
- 默认删除 source record 不删除 PDF asset。
- 勾选 `delete_pdf_asset` 后，受控 asset 文件被删除；共享或越界 asset 拒绝删除。
- 有 claims/citations/evidence refs 的 paper 删除被阻止，并列出阻塞项。
- `purge_discarded_papers` 不影响非 discarded papers。

#### 0.1.10 推荐里程碑

```text
M1：重排左侧信息架构
M2：补齐 tree backend 操作
M3：实现 Paper Library Tree custom component
M4：右键菜单 + 创建 / 重命名 / 删除 / 移动 collection
M5：多选论文 -> Add selected papers here
M6：导入论文时选择 / 创建 collection
M7：paper card 拖拽到 tree
M8：单篇论文真实删除 Danger Zone
M9：模型 technical meta 抽取
M10：Technical Taxonomy 自动视图
M11：Graph Cluster / Library Map
```

建议排期：

```text
Week 1
  - 重构左侧导航分组
  - 后端增加 rename / position / trash / purge / reorder
  - 单元测试覆盖树操作

Week 2
  - 做 paper_library_component
  - 接入右键菜单
  - 替换 Manage collections expander
  - 支持 collection create / rename / move / delete

Week 3
  - 批量选择论文后 Add selected papers here
  - 详情面板 collection picker 改造
  - Search 导入默认进入当前 collection

Week 4
  - React paper list 或轻量拖拽桥接
  - 支持 paper card 拖入 collection
  - Playwright 覆盖拖拽和右键菜单

Week 5+
  - 删除 paper record / PDF asset / artifacts 的危险区
  - purge discarded papers 预览和确认
  - technical meta 抽取
  - Methods / Topics / Datasets 自动视图
  - AI suggest location / split node / merge node

Later
  - graph clustering
  - method lineage
  - claim / citation network
```

#### 0.1.11 测试计划

单元测试：

- create node。
- rename node。
- move node into parent。
- position node before / after sibling。
- reject move under descendant。
- trash node with papers -> move to parent。
- trash node with papers -> move to unsorted。
- purge node removes source refs。
- move papers replace primary node。
- move papers append secondary node。
- `paper_rows()` node filter remains correct。
- delete paper preview reports PDF / artifacts / blocking refs。
- delete paper record removes only source by default。
- delete paper with referenced claims/citations is blocked。
- delete PDF asset refuses shared or out-of-root asset refs。
- purge discarded papers only touches `status=discarded` sources。

组件测试：

- context menu opens on right click。
- create collection emits correct event。
- rename emits correct event。
- drag node into another node emits move event。
- drag node before sibling emits reorder event。
- invalid drop shows no action。

E2E：

- right click Collections -> New collection -> appears in tree。
- right click node -> Rename -> title updates。
- drag child collection under another collection -> tree persists after reload。
- select papers -> right click node -> Add selected papers here -> paper count updates。
- open current collection -> imported paper appears there。

最终验收：

```text
1. 在左侧右键 Collections，创建 VLA。
2. 右键 VLA，创建 Memory。
3. 搜索论文时选择导入到 VLA / Memory。
4. 对已有论文多选，右键 VLA / Memory，选择 Add selected papers here。
5. 把 Memory 拖到另一个 topic 下。
6. 重命名 collection，不影响论文和 Reader。
7. 删除 collection 时选择把论文移动到父节点或 Unsorted。
8. 用 Work Queue 处理 PDF Missing / Needs Extraction，但不会把这些当成资料库结构。
9. 在 Danger Zone 删除单篇 paper 时，必须先看到 preview 和二次确认。
```

产品成功标准：

> 用户管理论文时感觉自己在整理一个研究资料库，而不是在操作一堆状态筛选器。

### 0.2 Paper Library 迁移到 8502 Research Workspace 计划

2026-05-22 判断：当前 Paper Library 已经完成 Tree-first、Manage Collections、paper drag/drop 和 8503 功能闭环自测，但页面仍然是混合架构：

- 左侧 tree 和中间 paper list 在 React Streamlit component iframe 中。
- 右侧 paper detail、PDF 状态、segment/annotation/claim/citation 指标和 quick actions 仍在 Streamlit column 中。
- 所有高频操作都要经过 `React iframe -> Streamlit event -> YAML/core -> rerun -> iframe remount`。
- Reader 已经在 `8502` FastAPI sidecar 内，Library 仍在 `8503` Streamlit 内，阅读工作流被两个 runtime 切开。

结论：

- **短期先把右侧状态和 quick actions 迁入同一个 React canvas**，让 `tree / paper list / detail panel` 变成一个完整工作台。
- **中期把 Paper Library 主体验迁到 `8502`**，作为 FastAPI + React 的 `/paper-library` 页面。
- **长期让 `8502` 承载 Research Workspace 的高交互路径**：Library、Reader、extraction queue、claims review 和 citation review；`8503` 保留 Home、profile、配置、低频管理和 fallback。

2026-05-22 执行状态：

- 已完成 shared workspace service：`src/nblane/core/paper_library_workspace.py` 负责 payload 和事件处理。
- 已完成 8502 API 主路径：`GET /paper-library`、`GET/POST /api/research/{profile}/paper-library...`。
- 已完成 standalone React 页面：同一套 component 支持 Streamlit iframe runtime 和 8502 standalone runtime。
- 已完成 tree / paper list / detail panel 同 canvas 的主体验，右侧状态和 quick actions 不再依赖 Streamlit column。
- 已完成 8503 入口按钮：Research 页面可一键打开 `8502 /paper-library`。
- 已完成真实删除论文 Danger Zone：8502 standalone 支持 preview、二次确认、source record 删除和可选清理 PDF / artifacts。
- 尚未完成：8502 E2E 截图、feature flag 默认切换策略、Reader split view、virtualized paper list。

#### 0.2.1 产品目标

目标体验：

```text
8502 /paper-library

Metrics / Find and import papers / Search / Sort
┌──────────────────┬────────────────────────────┬──────────────────────┐
│ Library Tree     │ Paper List                  │ Paper Detail          │
│                  │                            │                      │
│ Library          │ Cards / bulk selection      │ Title / authors       │
│ Collections      │ Drag paper to tree          │ Badges / status       │
│ Technical Taxon  │ Context menu                │ PDF state             │
│ Work Queue       │ Queue filters               │ Reader progress       │
│ System           │                            │ Quick actions         │
└──────────────────┴────────────────────────────┴──────────────────────┘
```

用户目标：

- 在一个工作台中完成 `找论文 -> 放入 collection -> 看状态 -> 打开 Reader -> 整理 claim/citation`。
- 选中 paper 后，右侧 detail panel 立即更新，不依赖 Streamlit rerun。
- 拖拽、右键、批量选择、quick actions 和 Reader 状态属于同一个 UI 状态机。
- Work Queue 继续服务处理流程，Collections 继续服务长期认知结构。

非目标：

- 不把整个 nblane Web UI 一次性迁出 Streamlit。
- 不改变 profile 目录、YAML 文件和 Reader artifacts 的数据存储方式。
- 不把 PDF、annotations、segments、claims、citations 跟随 collection 移动。
- 不在第一阶段重做 Paper Search、Graph Cluster 或 Technical Taxonomy 深层能力。

#### 0.2.2 目标架构

端口职责：

```text
8503 Streamlit
  - Home / Dashboard
  - Profile / AI config / low-frequency admin
  - Research Overview
  - Paper Library fallback / debug
  - 入口按钮或 iframe：Open Paper Library Workspace

8502 FastAPI + React
  - /paper-library
  - /reader/view/{source_id}
  - /api/research/{profile}/paper-library
  - /api/research/{profile}/paper-library/events
  - /api/research/{profile}/papers/{source_id}
  - /api/research/{profile}/reader/...
```

前端结构建议：

```text
src/nblane/paper_library_component/frontend/
  src/
    main.jsx
    payload.js
    style.css
    components/
      LibraryTree.jsx
      PaperList.jsx
      PaperDetailPane.jsx
      PaperMetricsBar.jsx
      PaperImportBar.jsx
      ContextMenu.jsx
      Dialog.jsx
      SplitLayout.jsx
    api/
      streamlitBridge.js
      httpClient.js
```

同一套 React 代码支持两种 runtime：

- Streamlit component runtime：继续通过 `Streamlit.setComponentValue(event)` 回传事件。
- 8502 standalone runtime：通过 HTTP API 读取 payload 和提交事件。

运行模式用 bootstrap payload 区分：

```js
window.__NBLANE_PAPER_LIBRARY_BOOTSTRAP__ = {
  runtime: "standalone", // streamlit | standalone
  profile: "王军",
  payload: {}
};
```

后端服务层建议：

```text
src/nblane/core/research_papers.py
  - 继续保留 tree / paper / row / move / trash / restore / purge 核心函数

src/nblane/core/paper_library_workspace.py
  - build_paper_library_payload(profile, view, node_id, query, sort, detail_id)
  - handle_paper_library_event(profile, event)
  - build_paper_detail_payload(profile, source_id)
  - build_paper_metrics_payload(profile)

src/nblane/web_reader_api/__init__.py
  - 当前 FastAPI routes 所在位置
  - 后续可拆到 src/nblane/web_reader_api/paper_library.py
```

#### 0.2.3 阶段一：右侧状态迁入同一个 React canvas（已落地，剩余 polish）

先不改端口，把当前 `8503` 中的 Paper Library component 升级成完整三栏 canvas。

改动范围：

- `pages/7_Research.py`
- `src/nblane/paper_library_component/frontend/src/main.jsx`
- `src/nblane/paper_library_component/frontend/src/payload.js`
- `src/nblane/paper_library_component/frontend/src/style.css`
- `src/nblane/core/paper_library_workspace.py`
- `src/nblane/core/research_papers.py`

任务：

- 新增 `PaperDetailPane`，把右侧 Streamlit detail panel 的核心信息迁入 React：
  - title
  - authors / year / collection path
  - badges
  - source id
  - PDF status
  - last page
  - segment count
  - annotation count
  - research claim count
  - research citation count
  - notes / summary preview
- `_paper_library_component_payload()` 增加：
  - `metrics`
  - `detail`
  - `reader`
  - `quick_actions`
  - `diagnostics`
- 保留 Streamlit 右侧 `_render_paper_detail_panel()` 作为 fallback 或 Advanced expander，不再作为主路径。
- quick actions 改为 component event：
  - `paper_library_sync_reader_tab`
  - `paper_library_run_extraction`
  - `paper_library_auto_chunk`
  - `paper_library_update_papers_status`
  - `paper_library_add_selected_papers_to_collection`
  - `paper_library_move_selected_papers_to_collection`
  - `paper_library_remove_papers_from_collection`
- component 内部维护当前 selected paper，点击 card 后 detail pane 立即响应。
- Streamlit rerun 后，payload 继续以 `detail_id` 恢复选中状态。
- 状态 chip 明确显示 `Reading / Archived / Discarded`，避免把 `Discard` 误解成删除。

验收：

- `8503/Research -> Paper Library` 中，tree/list/detail 都在同一个 iframe canvas 内。
- 点击不同 paper，右侧 detail pane 无明显闪动。
- Open Reader、Reader tab、Run extraction、Auto chunk 事件仍能触发原后端行为。
- 右侧 detail pane 在窄屏下可以折叠或变为 drawer。
- 旧 Streamlit detail panel 不影响主体验。

#### 0.2.4 阶段二：抽出 Paper Library Workspace payload 和 event handler（已落地）

payload 构造和 event handler 已从 `pages/7_Research.py` 抽成可复用 core/service；
8503 Streamlit 和 8502 FastAPI 共用同一套逻辑。

当前模块：

```text
src/nblane/core/paper_library_workspace.py
```

核心 API：

```python
def build_paper_library_payload(
    profile: str | Path,
    *,
    current_view: str = "all",
    current_node: str = "",
    query: str = "",
    sort_mode: str = "recent",
    selected_paper_ids: list[str] | None = None,
    detail_id: str = "",
    user_id: str = "local",
    reader_base: str = "",
) -> dict[str, object]:
    ...

def handle_paper_library_event(
    profile: str | Path,
    event: dict[str, object],
    *,
    selected_paper_ids: list[str] | None = None,
) -> dict[str, object]:
    ...
```

事件处理目标：

- Streamlit 和 FastAPI 共用同一套事件处理逻辑。
- 事件结果返回结构化 response，而不是直接 `st.success()` / `st.error()`：

```json
{
  "ok": true,
  "message": "Saved",
  "changed": {
    "sources": ["source:..."],
    "nodes": ["paper-node:..."]
  },
  "next": {
    "view": "all",
    "node_id": "paper-node:vla",
    "detail_id": "source:..."
  }
}
```

需要处理的兼容点：

- Streamlit session state 中的 `view/node/detail/bulk_select` 仍然由 page 保存。
- FastAPI runtime 的 `view/node/detail/query/sort` 由 URL search params 或 local state 保存。
- 文件写入仍走现有 core 函数和 `git_backup.record_change()`。
- 对 `research/sources.yaml` 的写入保留 file snapshot / conflict guard 能力。

验收：

- `pages/7_Research.py` 中 component payload 构造明显变薄。
- 同一个 unit test 可以直接调用 `build_paper_library_payload()` 验证 tree/list/detail。
- 同一个 unit test 可以调用 `handle_paper_library_event()` 验证 create/rename/move/drop/trash/restore/purge。
- 8503 的既有 e2e 闭环不回退。

#### 0.2.5 阶段三：8502 新增 Paper Library API（已落地，后续可拆路由模块）

FastAPI sidecar 已新增 Paper Library API，当前实现先放在 `web_reader_api/__init__.py`；
后续如果文件继续膨胀，再拆到 `web_reader_api/paper_library.py`。

改动范围：

- `src/nblane/web_reader_api/__init__.py`
- `src/nblane/web_reader_api/paper_library.py`（可选后续拆分）
- `src/nblane/web_reader_api/templates/`
- `tests/test_web_reader_api.py`

建议路由：

```text
GET  /paper-library
GET  /api/research/{profile}/paper-library
POST /api/research/{profile}/paper-library/events
GET  /api/research/{profile}/papers/{source_id}
POST /api/research/{profile}/papers/{source_id}/reader-token
```

payload 查询参数：

```text
view=all
node_id=paper-node:vla
query=diffusion
sort=recent
detail_id=source:research:...
```

事件提交示例：

```json
{
  "action": "paper_library_drop_papers_to_collection",
  "event_id": "20260522-...",
  "payload": {
    "node_id": "paper-node:vla",
    "paper_ids": ["source:research:20260519-003"],
    "append": false
  }
}
```

API 规则：

- profile 必须通过现有 profile allowlist / local admin 规则校验。
- 只允许访问 `profiles/{profile}` 内部文件。
- 不允许事件 payload 传入任意文件路径。
- 所有写操作返回 fresh payload version 或 changed ids，前端可局部刷新。
- Reader URL 继续使用现有 `mint_reader_token()` 机制。
- 后续新增真实删除事件时，继续走 `/events`，但必须先返回 delete preview 和阻塞项。

验收：

- `curl http://127.0.0.1:8502/api/research/王军/paper-library` 返回完整 JSON payload。
- `POST /events` 可以创建 collection、移动 paper、删除/恢复 collection。
- API 单测覆盖 profile 校验、未知 source、未知 node、非法 parent。
- 不启动 Streamlit 时，8502 仍可读取和更新 Paper Library。

#### 0.2.6 阶段四：8502 新增 standalone Paper Library 页面（已落地）

把 React workbench 作为 8502 的原生页面运行。

页面入口：

```text
http://127.0.0.1:8502/paper-library?profile=王军
```

模板建议：

```text
src/nblane/web_reader_api/templates/paper_library.html
src/nblane/paper_library_component/frontend/static/
```

前端行为：

- 首屏请求 `/api/research/{profile}/paper-library`。
- URL search params 记录 `view/node_id/detail_id/query/sort`，刷新后可恢复。
- collection 和 paper 操作调用 `/events`。
- 操作成功后局部刷新 tree counts、paper list 和 detail。
- 右侧 `Open Reader` 支持：
  - 新 tab 打开 `/reader/view/{source_id}?token=...`
  - 或在未来 split pane 内嵌 Reader iframe。

布局要求：

- 三栏宽度可调。
- detail pane 可折叠。
- paper list 可滚动，tree 保持位置。
- 顶部 metrics 固定在工作台上方，不占用过多垂直空间。
- 不使用 Streamlit 主题 class，CSS 独立。

验收：

- 直接打开 `8502/paper-library?profile=王军` 可以完成 8503 已通过的闭环：
  - create collection
  - rename collection
  - create subcollection
  - drag collection
  - drag paper
  - right-click paper add/move
  - select collection filter
  - trash / restore / purge collection
  - final diagnostics clean
- 页面没有 Streamlit rerun 抖动。
- Reader token 和 Open Reader 正常。

#### 0.2.7 阶段五：8503 Research 页面接入新工作台（部分落地）

当 8502 standalone 页面稳定后，调整 8503 Research 页面。

入口策略：

```text
Paper Library tab
  - primary button: Open Paper Library Workspace
  - optional iframe: Embed 8502 Paper Library
  - fallback expander: Streamlit Paper Library fallback
```

建议顺序：

1. 先加明显的 `Open Paper Library Workspace` 按钮。
2. 保留当前 8503 component 作为 fallback。
3. 用 feature flag 控制是否默认 iframe 嵌入 8502：

```text
NBLANE_PAPER_LIBRARY_RUNTIME=streamlit_component|fastapi_iframe|fastapi_link
```

验收：

- 用户从 `8503/Research` 能一键打开 `8502/paper-library`。
- 当前 profile 自动带入。
- 8502 不可用时，8503 fallback 仍然可用。
- docs/zh/guides/web-ui.md 更新启动和访问说明。
- feature flag 未设置时，默认仍以 link 打开 8502，不强制 iframe。

#### 0.2.8 阶段六：Reader 与 Library 深度联动

8502 承载 Paper Library 后，再逐步把 Reader 状态作为同一工作台的一部分。

##### 0.2.8A Reader 入口收敛：移除 Reader 内全库选论文

2026-05-22 产品判断：Reader 页顶部的 `Change Reader paper` selectbox 不应该作为主路径继续存在。
论文很多时，全库下拉无法承载 collection、work queue、搜索、状态和最近阅读语境；
从 Paper Library 打开 Reader 后，Reader 再要求或暗示用户重新选择论文，也会让
`Open Reader` / `Use in Reader tab` 显得重复。

产品边界：

- **Paper Library 是论文选择入口**：负责找论文、筛选、整理 collection、查看状态、决定读哪篇。
- **Reader 是单篇论文阅读现场**：负责当前 `source_id` 的 PDF、翻译、批注、问答、chunk、claim 和 citation。
- **Reader 不承担全库浏览**：大量论文切换应回到 Library 的 tree/search/filter/virtualized list，或未来走全局 command palette。
- **Library -> Reader 是主路径**：用户在 Library 选定论文后，Reader 直接打开该论文，不再在 Reader 顶部出现全库选择器。

短期任务：

- 在 `pages/7_Research.py::_render_reader()` 中移除或隐藏 `Change Reader paper` expander 和全库 `selectbox` 主路径。
- Reader tab 只读取以下来源决定当前论文：
  - `paper_reader_source:{profile}` 中由 Library 明确写入的 source id。
  - 未来 URL / route 中的 source id。
  - 最近阅读论文作为 `Continue reading` fallback。
- 如果没有当前论文，不自动选择一篇陌生论文；显示空状态：
  - `Open Paper Library`
  - `Continue recent paper`（仅当存在最近阅读）
  - `No paper selected`
- Paper Library 的 `Open Reader` 成为主动作：
  - 有 PDF 时打开 `/reader/view/{source_id}`。
  - 无 PDF 时仍可进入 metadata/text fallback 或提示先绑定 PDF。
- `Use in Reader tab` 降级为 legacy/fallback，后续在 8502 standalone 稳定后移除。
- Reader 顶部增加 `Back to Library` / `Open in Library`：
  - 返回时恢复 `view/node_id/query/sort/detail_id`。
  - 不丢失用户在 Library 中的 collection 或 work queue 语境。
- Reader 不再无条件覆盖 `Paper Library detail` 选中项；只有来自 Library 的显式打开动作才同步 detail。

中期任务：

- 8502 `/paper-library` 支持 `Open in split reader`，在同一工作台内打开 Reader iframe。
- split reader 模式下：
  - 左侧保持 Library tree。
  - 中间 paper list 或右侧 detail 切换为 Reader。
  - paper selection 与 reader source 双向同步，但同步源必须明确。
- Reader 保存 annotation / translation / chunk / claim 后，Library detail 局部刷新计数。
- 支持最近阅读列表和 `Continue reading`，但它们是快捷入口，不替代 Library 选论文。

验收：

- 从 Paper Library 点击 `Open Reader` 后，直接进入对应论文阅读，不需要再次选择论文。
- Reader 首屏主要空间给 PDF / translation / notes / AI，不被论文选择区占据。
- 论文很多时，选论文仍通过 Library 的 tree、search、filter 和虚拟列表完成。
- 直接打开 Reader 且没有当前论文时，不自动挑选默认论文，而是引导回 Paper Library。
- 返回 Library 后仍停留在原来的 collection / smart view / query / selected paper。
- 删除或隐藏 `Change Reader paper` 后，text fallback Reader 仍能打开当前论文。

任务：

- detail pane 显示 Reader 最近状态：
  - last page
  - visible pages
  - active tab
  - target language
  - translation coverage
  - segment coverage
- 支持 `Open in split reader`：
  - 左侧保持 tree
  - 中间或右侧切换成 Reader iframe
  - paper selection 和 reader source 同步
- Reader 产生 annotation / translation / chunk / claim 后，Library detail 自动刷新计数。
- Work Queue 中的 `Stale Translation`、`Needs Extraction`、`Claims Need Review` 与 Reader action 结果联动刷新。

验收：

- 在 Library 中打开 Reader，关闭 Reader 后仍回到同一个 selected paper。
- Reader 保存 annotation 后，Library detail 的 annotation count 更新。
- extraction 完成后，`Needs Extraction` count 下降，segments count 上升。

#### 0.2.9 测试计划

单元测试：

- `build_paper_library_payload()`：
  - metrics 正确。
  - tree sections 正确。
  - paper list 过滤和排序正确。
  - detail payload 正确。
- `handle_paper_library_event()`：
  - create / rename / move / reorder。
  - paper drop replace primary ref。
  - paper add append secondary ref。
  - trash with move_to_parent / move_to_unsorted / move_to collection。
  - restore / purge。
  - update status: reading / archived / discarded。
  - delete preview / delete paper record / delete PDF asset / purge discarded。
  - invalid event 返回 structured error。

API 测试：

- `GET /api/research/{profile}/paper-library`。
- `POST /events` 成功路径。
- unknown profile / source / node。
- illegal parent / descendant parent。
- Reader token generation。
- delete preview returns blocking refs and warnings。
- delete endpoints reject referenced papers unless advanced unlink policy is explicit。
- delete PDF asset rejects shared or out-of-root refs。

前端测试：

- payload normalize。
- tree context menu。
- detail pane 渲染长标题、中文标题、无 PDF、PDF ready。
- quick action event。
- drag target state。

E2E：

```text
8503 current path
  - 确认 fallback component 仍闭环。

8502 standalone path
  - 直接打开 /paper-library?profile=...
  - 完成 create / rename / child / drag collection / drag paper / add paper / filter / trash / restore / purge。
  - 对 discarded paper 打开 Danger Zone，完成 delete preview，确认默认不删 PDF。

8503 -> 8502 path
  - 从 Research tab 点击 Open Paper Library Workspace。
  - profile 正确传入。
  - Open Reader 正常。
```

截图检查：

- 1440x1000 desktop。
- 1280x800 laptop。
- 390x844 mobile/narrow。
- 长英文标题。
- 长中文标题。
- badges 很多。
- 空 collection。
- 无 PDF paper。
- PDF ready paper。

#### 0.2.10 风险和处理

风险：两套 runtime 事件逻辑分叉。

- 处理：抽 `paper_library_workspace.py`，Streamlit 和 FastAPI 都调用同一套 payload/event service。

风险：8502 standalone 绕过 Streamlit profile/session 状态。

- 处理：profile 显式放入 URL，并在 API 层做 allowed profile 校验；本地 admin 模式继续兼容。

风险：Reader token、profile 和 source_id 权限不一致。

- 处理：Open Reader 始终由 8502 API 生成 token，不让前端拼接敏感 payload。

风险：迁移期间用户不知道哪个入口是主路径。

- 处理：8503 Paper Library tab 明确展示 primary action；fallback 标注为 debug/legacy。

风险：大列表性能。

- 处理：8502 standalone 后引入 virtual list；8503 component 阶段先保持当前数量级可用。

风险：文件并发写入。

- 处理：写操作继续走现有 core 和 file snapshot guard；API response 返回 changed ids 和最新 updated/version。

风险：用户把 Discard 理解成 Delete，或误删有引用的论文。

- 处理：`Discard` 改名为 `Mark as discarded`；真实删除只放在 Danger Zone；删除前必须 preview + 二次确认；有引用默认阻止。

#### 0.2.11 推荐里程碑

```text
M1：PaperDetailPane 迁入 React component，同 canvas 三栏闭环（已完成主路径）
M2：抽出 paper_library_workspace.py，统一 payload/event service（已完成）
M3：8502 增加 Paper Library JSON API（已完成）
M4：8502 增加 /paper-library standalone React 页面（已完成）
M5：8503 Research 增加 Open Paper Library Workspace 入口（已完成 link，iframe/flag 待定）
M6：真实删除论文 Danger Zone 接入 8502 /events（已完成）
M7：8502 Playwright E2E + 截图验收（下一步）
M8：Reader 状态与 Library detail pane 联动
M9：split reader / inline reader preview
M10：virtualized paper list 和更完整 keyboard workflow
```

收尾顺序：

```text
Done
  - 迁移右侧 detail/status/quick actions 到 React canvas
  - 抽 payload/event service
  - 增加 8502 API routes
  - 增加 8502 standalone page
  - 8503 增加工作台入口

Next 1
  - 真实删除论文 Danger Zone
  - delete preview / delete source / delete PDF asset / purge discarded
  - blocked refs 和 warnings 单测

Next 2
  - Playwright 覆盖 8502 完整闭环
  - 1440 / 1280 / mobile 截图检查
  - docs/zh/guides/web-ui.md 更新访问说明

Next 3
  - feature flag：streamlit_component / fastapi_link / fastapi_iframe
  - 8502 不可用时 fallback 提示

Later
  - Reader split view
  - task progress
  - virtualized list
  - Paper Search import wizard 迁入 8502 canvas
```

最终验收：

```text
1. 不打开 8503，只打开 8502 /paper-library，可以完成 Paper Library 主闭环。
2. 右侧状态、quick actions、Reader 入口和 paper selection 在同一个 canvas 内同步。
3. 8503 Research 页面可以一键跳到 8502 工作台，并保留 fallback。
4. Tree-first / Manage Collections / drag-drop / delete policy 行为与 8503 已验证闭环一致。
5. Open Reader 仍然使用 8502 sidecar token，PDF Reader 主路径不回退。
6. 所有写入仍落到 profile YAML 和现有 artifacts，不引入第二套数据源。
7. `Mark as discarded` 和 `Delete paper...` 在 UI、API 和测试中明确分离。
```

产品成功标准：

> 用户不再感觉 Paper Library 是嵌在 Streamlit 表单里的小组件，而是一个和 Reader 连在一起的论文研究工作台。

## 1. 背景和目标

当前 Research Reader 已具备 PDF.js 阅读、highlight/comment、translation、review card、deep read、GROBID/PyMuPDF 抽取和 FastAPI sidecar 等基础能力，但阅读体验还没有形成一个稳定的“论文工作台”：

- 中文翻译尝试贴合 PDF rect 绝对定位，遇到表格、多栏和窄列时会被挤碎，基本不可读。
- compare/follow 主要按页同步，不够贴合真实段落阅读。
- 选区 toolbar 不能拖拽或固定，有时遮挡正文。
- 早期选区按钮文案偏内部对象名，用户不容易判断动作结果。
- highlight comment 点击后直接进入重编辑态，阅读 comment 太重，且点击页面空白不能自然退出。
- 侧边 panel 没有统一承载 comment、翻译结果和 review 信息。
- 多个分析入口分裂，用户心智上其实都是“分析这篇论文”。
- 论文载入时会等待抽取或 payload 构建，应该分阶段载入，先读 PDF，再后台补齐结构化文本、翻译和分析。
- GROBID 后台处理 PDF 很快，应该进入默认后台准备流程，而不是阻塞主阅读入口。

目标是把 Reader 升级为一个接近前沿论文阅读工具的可追溯工作台：PDF 立即打开，GROBID/PyMuPDF 后台准备结构化内容，翻译按段落流可读展示，批注和译文统一进入侧栏 review，AI 输出始终带 refs，用户可以快速判断论文价值，也可以继续深读和沉淀 claim/citation。

## 2. 设计原则

- **先阅读，后增强**：打开论文时先显示 PDF 和已有缓存，不因抽取、翻译、分析阻塞。
- **翻译可读优先于像素级复刻**：中文默认按 section/paragraph/table/caption 流式排版；原位 overlay 只作为 debug 能力。
- **所有 AI 输出可追溯**：翻译、解释、问答、分析都必须绑定 segment / annotation / chunk / citation refs。
- **批注先看再改**：highlight comment 默认进入阅读卡片，只有点击 Edit 才进入编辑态。
- **按钮说人话**：把内部对象名转成用户动作，比如 `Save quote`、`Cite this`。
- **长任务全部后台化**：全文翻译、GROBID 准备、Analyze Paper 走 task + SSE/polling，完成后刷新 payload。

## 3. 开发范围

优先主路径：

- `src/nblane/web_reader_api/templates/index.html`
- `src/nblane/web_reader_api/__init__.py`
- `src/nblane/core/research_papers.py`
- `src/nblane/core/reader_actions.py`
- `src/nblane/core/reader_tasks.py`
- `pages/7_Research.py`
- `tests/test_web_reader_api.py`
- `tests/test_research_papers.py`
- `tests/test_research_paper_reader_component.py`（仅事件契约和 retired runtime）

当前以 FastAPI sidecar Reader 为唯一主体验。`research_paper_reader_component` 的
Streamlit runtime 不再承载 PDF Reader，只保留 `events.py` 作为 action/state 契约来源。

## 4. Phase 1：分阶段载入和后台准备

### 4.1 行为

打开 Reader 时：

1. FastAPI `/payload` 只返回最小上下文：source、PDF URL、当前页附近 pages/segments/translations、已有 annotations、已有 analysis 摘要。
2. PDF range endpoint 立即可用，PDF.js 先渲染当前页。
3. 如果 pages/segments 缺失或 PDF fingerprint 变化，后台启动 `prepare_reader_artifacts`。
4. 顶部显示阶段状态：
   - `PDF ready`
   - `Extracting page text`
   - `Running GROBID`
   - `Structured text ready`
   - `Fallback text ready`
   - `Preparation failed`
5. 后台完成后刷新 payload，但不打断当前滚动位置。

### 4.2 后端任务

新增 reader task action：

```text
prepare_reader_artifacts
```

输入：

```json
{
  "source_id": "...",
  "page": 1,
  "prefer_grobid": true,
  "event_id": "..."
}
```

输出：

```json
{
  "summary": {
    "ready": true,
    "status": "ready|fallback|failed|missing_pdf",
    "pages": 12,
    "segments": 180,
    "structure_backend": "grobid|pymupdf_fallback|fallback",
    "warnings": []
  }
}
```

进度：

```json
{
  "phase": "extracting_pages|running_grobid|saving_segments|done",
  "label": "Running GROBID...",
  "current": 1,
  "total": 3,
  "saved": 0
}
```

### 4.3 关键实现

- `pages/7_Research.py` 上传 PDF / 下载 PDF 后，不再同步等待完整 structured extraction；只保存 PDF asset 和 page count，然后提示后台准备。
- `web_reader_api.__init__._payload_for_context()` 不再 `requested_pages=set(range(1, total_pages + 1))`，改成当前页窗口，例如 `{page-1,page,page+1}`。
- `reader_tasks.ALLOWED_READER_TASK_ACTIONS` 增加 `prepare_reader_artifacts`。
- `reader_actions` 处理 `prepare_reader_artifacts`，调用 `ensure_paper_reading_artifacts(..., prefer_grobid=True)` 并发送 progress callback。
- 前端 `loadReaderPayload({preserveView:true})` 完成后不重建 PDF 源，只刷新 annotations/translations/analysis/panel。

### 4.4 验收

- 打开一篇刚上传的 PDF，PDF 页面先显示，不等待 GROBID。
- 后台准备完成后，侧栏 segments/translation 可用。
- GROBID 失败时仍可阅读 PDF，并显示 fallback warning。
- `/payload` 响应不包含全论文所有 segments/translations。

## 5. Phase 2：翻译阅读流重构

### 5.1 行为

默认翻译视图改为“中文阅读流”，而不是按 PDF rect 覆盖：

- 右侧按 `section_path -> paragraph/order` 分组。
- 每个译文块显示 locator，例如 `p. 3 · § Method`。
- 状态显示 `translated / missing / stale / failed`。
- missing/stale 块显示操作：`Translate` 或 `Retry`。
- 点击译文块跳到 PDF 页并高亮对应 rect。
- 点击 PDF 中的 segment 或 annotation，右侧滚到对应译文。
- 表格不做窄框绝对定位；GROBID 能识别 table 时显示 table/caption card，否则显示页面文字块和页码。

### 5.2 前端改动

新增：

```js
function translationFlowHtml() {}
function translationUnitGroups() {}
function activeSegmentFromViewport() {}
function buildAnchorIndex() {}
function setActiveAnchor(anchorId, options = {}) {}
```

调整：

- `translationReaderHtml()` 默认调用 flow renderer。
- `renderTranslationPage()` 和 `.pr-translation-block.placed` 保留为 overlay mode，但不作为默认。
- `syncCompareScroll()` 从 page-level 同步改为 anchor-level 同步。
- `wireTranslationRows()` 支持 hover 高亮、click 跳转、retry missing/stale。

### 5.3 CSS

新增译文流样式：

```css
.pr-translation-flow {}
.pr-translation-section {}
.pr-translation-unit {}
.pr-translation-unit.active {}
.pr-translation-unit-status {}
.pr-translation-locator {}
.pr-translation-source-snippet {}
```

减少默认 overlay 强制定位：

- `.pr-translation-block.placed` 只在 `readerMode` 或 translation layout mode 为 `overlay` 时出现。

### 5.4 验收

- 截图中的表格翻译不再变成逐词竖排。
- 当前页中文译文能连续阅读。
- 点击译文能跳回 PDF 原文页。
- stale 翻译不会覆盖旧译文，且 UI 明确提示重新翻译。

## 6. Phase 3：可拖拽 toolbar 和清晰动作命名

### 6.1 新文案

选区 toolbar 按以下顺序显示：

```text
Translate
Highlight
Note
Explain
Ask with selection
Save quote
Cite this
```

含义：

- `Translate`：翻译当前选区。
- `Highlight`：只保存高亮。
- `Note`：高亮并添加 comment。
- `Explain`：解释当前选区的术语、难句、公式上下文。
- `Ask with selection`：把选区作为上下文向整篇论文提问。
- `Save quote`：保存为可复用 research chunk。
- `Cite this`：创建 claim/citation 候选。

保留后端 action 名：

- `create_chunk_from_selection`
- `create_citation`
- `explain_selection`
- `ask_paper`

只改 UI 文案和前端 action 分组，不改存储语义。

### 6.2 拖拽行为

Toolbar 支持：

- 拖拽移动。
- pin/unpin。
- close。
- reset position。
- 底部 dock fallback。

local/session state 增加：

```json
{
  "dock_position": {"x": 320, "y": 120},
  "dock_pinned": true,
  "dock_layout": "floating|bottom|selection"
}
```

### 6.3 验收

- toolbar 可以被鼠标拖到不遮挡阅读的位置。
- 刷新当前 reader 后位置保持。
- Escape 或 close 能关闭 toolbar。
- 内部对象名不再作为裸按钮文案出现。

## 7. Phase 4：Comment 阅读态 / 编辑态拆分

### 7.1 行为

点击 highlight：

1. 默认打开 comment 阅读卡片。
2. 展示 selected text、note/comment、tags、locator、chunk refs。
3. 操作按钮：`Edit`、`Save quote`、`Cite this`、`Delete`。
4. 点击空白处关闭。
5. 点击 `Edit` 后进入 textarea 编辑态。
6. 编辑态 Save / Cancel；未修改时点击空白可关闭，有修改时保留确认操作。

### 7.2 前端状态

替换当前：

```js
let annotationPopover = { open, annotation, pending, x, y }
```

为：

```js
let annotationPopover = {
  open: false,
  mode: "read", // read | edit
  annotation: null,
  pending: false,
  dirty: false,
  x: 16,
  y: 16
}
```

### 7.3 侧栏 Notes

Notes tab 显示：

- comment 优先，其次 selected text。
- page locator。
- tag/chunk/citation 状态。
- 当前页过滤和 all/filter toggle。

### 7.4 验收

- 点击有 comment 的 highlight 可以轻量阅读。
- 点击页面空白可以关闭阅读卡片。
- 只有点 Edit 才进入编辑框。
- 新建 note 仍然可以从选区 toolbar 进入编辑态。

## 8. Phase 5：统一侧栏 panel

Tabs 调整为：

```text
Notes | Translation | Review | Claims
```

### 8.1 Notes

- annotations 列表。
- comment 摘要。
- jump/edit/delete。
- current page/all filter。

### 8.2 Translation

- 当前页或当前 section 的翻译流。
- missing/stale 统计。
- translate visible / retry stale。
- 点击译文跳 PDF。

### 8.3 Review

- TL;DR。
- key points。
- method / experiments。
- limitations。
- score grid。
- project relevance。
- reading plan。
- cited refs。

### 8.4 Claims

- chunks。
- citation candidates。
- research claims。
- 从条目跳回 PDF locator。

### 8.5 验收

- 用户可以在侧栏统一阅读 comment 和译文。
- Analyze Paper 输出能直接显示在 Review tab。
- Claims tab 能看到从选区创建的 quote/citation。

## 9. Phase 6：Analyze Paper 合并（已完成）

### 9.1 新按钮

顶部已去掉旧的并列分析入口：

```text
Review
Codex long read
```

改为一个：

```text
Analyze Paper
```

### 9.2 后端 action

新增：

```text
analyze_paper
```

兼容旧 action：

- `generate_review_card`
- `codex_deep_read`

旧 action 保留 API 兼容，但 UI 不作为主按钮展示。

### 9.3 输出结构

保存到 `research/analysis/<source>.yaml`：

```yaml
schema_version: "1.0"
source_id: ...
updated: ...
tldr: ...
key_points: []
method: []
experiments: []
limitations: []
usefulness: ...
scores:
  novelty: 0
  technical_depth: 0
  evidence_quality: 0
  reproducibility: 0
  relevance: 0
  overall: 0
score_rationale: []
project_relevance: []
reading_plan: []
open_questions: []
cited_segment_refs: []
cited_chunk_refs: []
cited_annotation_refs: []
warnings: []
```

### 9.4 AI 行为

- 默认先用 direct LLM 生成 review card。
- 如果 Codex local backend 可用，可附加更长的 deep reading findings。
- 结果合并后一次写入 analysis。
- 所有 substantive claim 和评分理由必须带 refs；无 refs 时降级为 warning。

### 9.5 验收

- 一次点击生成关键信息和评分。
- Review tab 不需要用户理解旧分析入口之间的差别。
- 旧 API 测试仍通过。

## 10. 测试清单

### 10.1 单元测试

- `build_reader_payload` 只返回上下文窗口，不返回全论文。
- GROBID 可用时生成 structured segments 和 refs。
- GROBID 不可用时 fallback，不阻塞 PDF。
- stale translation 不覆盖现有译文。
- `analyze_paper` 输出包含 scores 和 cited refs。

### 10.2 API 测试

- `/tasks` 支持 `prepare_reader_artifacts`。
- `/tasks` 支持 `analyze_paper`。
- SSE snapshot 包含 phase/progress/refresh。
- task done 后前端可刷新 payload。

### 10.3 前端 contract 测试

- HTML 包含 draggable dock 状态。
- HTML 包含 annotation read/edit mode。
- HTML 包含 Translation panel tab。
- HTML 包含 Analyze Paper 按钮。
- HTML 不再暴露早期内部对象名式按钮文案。

### 10.4 手动验收

用一篇包含双栏、表格、图和 references 的 PDF：

1. 首次打开 PDF 立即显示第一页。
2. 后台 GROBID 状态可见。
3. 翻译当前页后中文可连续阅读。
4. 全文翻译不把表格翻译挤成竖排碎片。
5. 点击中文译文跳回 PDF 页。
6. 点击 highlight 先看 comment，点空白关闭。
7. toolbar 可拖拽移动。
8. Analyze Paper 一次生成 TL;DR、评分和阅读建议。
9. 新建 quote/citation 后可在 Claims tab 看到。

## 11. 风险和注意事项

- 静态组件 runtime 已退出主路径；后续 Reader 行为应继续收敛到 FastAPI sidecar 模板和 core handler。
- GROBID coordinates 解析可能不完整，不能把 overlay 作为普通翻译阅读方式。
- 全文翻译和 Analyze Paper 可能很慢，必须走后台任务，不能回到同步阻塞。
- AI activity 不能记录完整 PDF、全文翻译或完整 prompt，只记录摘要和 refs。
- PyMuPDF 许可边界仍需在部署文档中保持可见。
- `reader_actions.py` 和 `pages/7_Research.py` 有重复 action 处理逻辑，后续应逐步收敛到 core handler，减少行为分叉。

## 12. 参考方向

- Zotero PDF Reader：批注侧栏、note 和 PDF locator 的成熟模式。
  <https://www.zotero.org/support/pdf_reader>
- Semantic Reader：TOC、citation cards、skimming/highlight 等阅读增强。
  <https://www.semanticscholar.org/product/semantic-reader>
- Semantic Scholar TLDR：快速判断论文是否值得深读。
  <https://www.semanticscholar.org/product/tldr>
- GROBID：学术 PDF 到 TEI、full text、references 和 coordinates。
  <https://grobid.readthedocs.io/en/latest/Grobid-service/>
  <https://grobid.readthedocs.io/en/latest/Coordinates-in-PDF/>
