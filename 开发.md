# Research Reader 临时开发计划

> 临时工作文档。用于把 Research Reader 的排版、批注、翻译、分析和分阶段载入问题拆成可执行开发任务；后续稳定后再合并进 `docs/zh/product/paper_reading.md` 或对应 roadmap。

> 2026-05-21 更新：FastAPI sidecar Reader 是唯一 PDF Reader 主路径；旧 Streamlit 组件启动路径已停用，仅保留事件常量契约用于兼容测试。

## 0. 2026-05-21 Paper Library 工作台改造记录

本轮已把 `pages/7_Research.py` 的 Paper Library 从数据库表格式页面升级为 Streamlit 版三栏工作台：

- 左侧常驻 Smart Views 和 Collections，支持 All Papers、Unsorted、Reading、PDF Missing、Needs Extraction、Claims Need Review、Duplicate Risk、Stale Translation、Recently Read、Private、Reviewed、Archived、Discarded 等队列。
- 中间区域改为可搜索、可排序、可批量选择的 paper card 列表，并保留高级 table view。
- 右侧详情面板显示论文标题、作者/年份/路径、PDF/segment/annotation/claim/citation 指标和快速动作。
- 快速动作已包含 Open Reader、同步到 Reader tab、Run extraction、Auto chunk、状态/节点/标签整理。
- `src/nblane/core/research_papers.py::paper_rows()` 已补充 workbench 需要的展示字段和视图筛选，新增测试覆盖 no-PDF、needs-extraction、duplicate-risk 与节点组合筛选。

本轮未完成，后续继续：

- **可拖拽 Collections Tree**：当前仍是 Streamlit 按钮树，不能像 blog outline 一样拖拽排序、拖入论文、右键重命名/移动/删除。
- **论文列表虚拟滚动和键盘操作**：paper card 已可用，但大量论文时仍依赖 Streamlit 全量渲染；需要自定义 component 支持虚拟列表、方向键选择、多选范围选择。
- **真正的详情抽屉**：右侧面板已常驻，但不是可收起/浮动 drawer；移动端和窄屏需要单独交互设计。
- **Reader 内联闭环**：Library 可以打开 Reader，但还没有从 Library 直接预览 PDF 页、选区创建 note/claim/citation，完整闭环仍在 sidecar Reader 内。
- **搜索 query grammar**：目前是关键词搜索；后续支持 `tag:robotics status:reading has:pdf claims:0 after:2026-05-01` 等结构化查询。
- **Duplicate merge 工作流**：现在只展示 duplicate-risk 队列；还缺少 compare metadata、选择主记录、合并 tags/notes/assets/artifacts 的安全流程。
- **Import wizard**：Paper Search 导入已有 library location，但还没有统一的 PDF / URL / BibTeX / arXiv 多入口导入向导。
- **Saved filters**：Smart Views 是系统预设；后续需要用户保存自定义筛选，并同步到 profile 级配置。
- **批量后台任务状态**：Bulk action 已能 move/status/tag，但批量 GROBID、批量 extraction、批量 claim generation 还需要 task queue 和进度反馈。
- **视觉 polish 和响应式验证**：当前是 Streamlit CSS 增强版；后续要用 Playwright/截图检查宽屏、窄屏、长标题、长 badge、中文标题和无 PDF 状态。

### 0.1 Paper Library Tree-first 和 Manage Collections 改造计划

当前 Paper Library 的 Smart Views 很适合处理系统任务，例如 PDF missing、Needs extraction、Duplicate risk，但它不适合作为长期论文资料库的主结构。用户真正需要记住的是论文在研究地图中的位置：属于哪个 project、topic、method、dataset、benchmark 或问题簇。因此后续改造目标是把左侧从“状态筛选列表”升级成“稳定的研究资料库树”，同时保留 Smart Views 作为工作队列。

核心原则：

- **Collections / Library Tree 是认知结构**：回答“这篇论文应该放在哪里”。
- **Smart Views / Work Queue 是处理结构**：回答“下一步要处理哪些论文”。
- **Technical Taxonomy 是模型辅助结构**：由模型 meta 抽取生成 topic、method、dataset、benchmark 等维度，但不能静默覆盖用户 collection。
- **Graph Cluster 是未来高级视图**：当 relation edges 足够多后，用图聚类表达论文之间的真实研究关系。
- **树是 virtual hierarchy，不是真实文件夹**：移动论文只改 `source.library_node_refs`，不移动 PDF、annotation、claim、citation 或 reader artifacts。

目标左侧结构：

```text
Library
  All Papers
  Unsorted Inbox
  Recently Read

Collections
  All collections
  <user collection tree>

Technical Taxonomy
  Topics
  Methods
  Datasets
  Benchmarks

Work Queue
  Reading
  PDF Missing
  Needs Extraction
  Claims Need Review
  Duplicate Risk
  Stale Translation

System
  Private Sources
  Reviewed
  Archived
  Discarded
```

#### 0.1.1 阶段一：重排左侧信息架构

先在现有 Streamlit 页面里完成信息架构重排，不等自定义组件。

改动范围：

- `pages/7_Research.py::_render_paper_library()`
- `pages/7_Research.py::_LIBRARY_VIEW_LABELS`
- `pages/7_Research.py::_paper_tree_buttons()`

任务：

- 把当前平铺的 Smart Views 拆成 `Library / Work Queue / System` 三组。
- 把 Collections 上移到 Work Queue 之前，作为主要资料库入口。
- 将 `Smart Views` 文案改为 `Work Queue` 或分组内标题。
- 保留当前所有 view id 和 `paper_rows(..., view=...)` 筛选逻辑，避免破坏现有行为。
- 当前节点选中后，中间列表 caption 显示 collection path，例如 `VLA / Memory`。

验收：

- 用户第一眼看到的是 `Library` 和 `Collections`，不是 PDF missing 等维护状态。
- `PDF Missing`、`Needs Extraction`、`Duplicate Risk` 等仍可点击，并显示正确数量。
- `All Papers`、`Unsorted Inbox` 和 collection 过滤行为保持兼容。

#### 0.1.2 阶段二：补齐 Paper Library Tree 后端操作

现有数据层已有 `PaperLibraryNode`、`PaperLibraryTree`、`upsert_paper_library_node()` 和 `move_papers_to_node()`，但 Manage Collections 需要完整的文件树操作 API。

改动范围：

- `src/nblane/core/research_papers.py`
- `tests/test_research_papers.py`
- `profiles/template/research/library-tree.yaml`

新增或补齐函数：

```python
create_paper_library_node(profile, title, parent_id="", description="", color="", icon="")
rename_paper_library_node(profile, node_id, title)
position_paper_library_node(profile, node_id, parent_id="", before_node_id="", after_node_id="")
reorder_paper_library_node(profile, node_id, direction)
trash_paper_library_node(profile, node_id, paper_policy="move_to_parent")
restore_paper_library_node(profile, node_id)
purge_paper_library_node(profile, node_id, paper_policy="move_to_unsorted")
remove_papers_from_node(profile, source_ids, node_id)
set_paper_primary_node(profile, source_id, node_id)
```

规则：

- 不能移动 root。
- 不能把节点移动到自己或自己的后代下面。
- `position_paper_library_node()` 支持 before / after / into，一次保存完成 parent 和 order 更新。
- sibling order 统一重排为 `10, 20, 30...`，避免长期拖拽后 order 混乱。
- 删除 collection 前必须选择论文处理策略：
  - 移动到父 collection。
  - 移动到 Unsorted Inbox。
  - 移动到指定 collection。
  - 取消删除。
- 删除 collection 不删除 source、PDF、annotations、segments、claims、citations。
- `library_node_refs[0]` 暂定为 primary location，后续如有需要再升级为 `library_links`。

可参考实现：

- `src/nblane/core/public_site.py::position_public_library_node()`
- `src/nblane/core/public_site.py::reorder_public_library_node()`

单元测试：

- create / rename / move / reorder / trash / purge node。
- reject self-parent 和 descendant-parent。
- 删除带论文的 collection 时，source refs 按策略迁移。
- `move_papers_to_node(..., append=False)` 替换 primary location。
- `move_papers_to_node(..., append=True)` 增加 secondary location。
- `paper_rows(profile, node_id=...)` 在移动节点和移动论文后仍正确。

#### 0.1.3 阶段三：实现真正的 Paper Library Tree 组件

纯 Streamlit button tree 无法自然支持右键菜单、拖拽排序、拖入论文、多选投递和键盘操作。需要新增一个 Paper Library 专用 Streamlit custom component，或把现有 blog library tree 组件抽象后复用。

新增目录建议：

```text
src/nblane/paper_library_component/
  __init__.py
  frontend/
    package.json
    vite.config.js
    src/
      main.jsx
      PaperLibraryTree.jsx
      PaperLibraryRow.jsx
      PaperLibraryContextMenu.jsx
      PaperLibraryDialog.jsx
      MoveTargetPicker.jsx
      dndHelpers.js
      payload.js
      style.css
```

可复用参考：

- `src/nblane/public_blog_editor_component/frontend/src/library/LibraryRow.jsx`
- `src/nblane/public_blog_editor_component/frontend/src/library/LibraryContextMenu.jsx`
- `src/nblane/public_blog_editor_component/frontend/src/library/dndHelpers.js`

组件输入 payload 示例：

```json
{
  "active_view": "all",
  "active_node_id": "paper-node:vla-memory",
  "selected_paper_ids": ["source:paper:a", "source:paper:b"],
  "sections": [
    {
      "id": "library",
      "title": "Library",
      "items": [
        {"id": "all", "type": "view", "title": "All Papers", "count": 8},
        {"id": "unsorted", "type": "view", "title": "Unsorted Inbox", "count": 2}
      ]
    },
    {
      "id": "collections",
      "title": "Collections",
      "items": [
        {
          "id": "paper-node:vla",
          "type": "collection",
          "title": "VLA",
          "count": 12,
          "children": [
            {"id": "paper-node:vla-memory", "type": "collection", "title": "Memory", "count": 4}
          ]
        }
      ]
    },
    {
      "id": "work_queue",
      "title": "Work Queue",
      "items": [
        {"id": "no_pdf", "type": "view", "title": "PDF Missing", "count": 4},
        {"id": "needs_extraction", "type": "view", "title": "Needs Extraction", "count": 4}
      ]
    }
  ],
  "capabilities": {
    "create_collection": true,
    "rename_collection": true,
    "move_collection": true,
    "delete_collection": true,
    "drop_papers": true
  }
}
```

组件返回事件：

```json
{
  "action": "paper_library_create_collection",
  "event_id": "20260521-...",
  "payload": {
    "title": "Memory",
    "parent_id": "paper-node:vla"
  }
}
```

事件清单：

```text
paper_library_select_view
paper_library_select_collection
paper_library_create_collection
paper_library_rename_collection
paper_library_move_collection
paper_library_reorder_collection
paper_library_trash_collection
paper_library_purge_collection
paper_library_drop_papers_to_collection
paper_library_add_selected_papers_to_collection
paper_library_move_selected_papers_to_collection
paper_library_remove_papers_from_collection
paper_library_import_paper_here
paper_library_suggest_subtopics
paper_library_suggest_locations
```

验收：

- collection 节点可展开 / 折叠。
- 支持搜索 collection。
- 支持右键菜单。
- 支持节点拖拽排序和拖拽到其他节点下。
- 支持选中论文后右键 collection 添加或移动。
- 原 `Manage collections` expander 不再是主要入口。

#### 0.1.4 阶段四：Manage Collections 人类化

现在的 `Manage collections` 表单要求用户手动填 node title、node id、parent id，不符合论文管理工具习惯。后续应改成文件管理器式交互。

Collections 根部右键菜单：

```text
New collection
Paste papers here
Expand all
Collapse all
Validate library
Export tree
```

普通 collection 右键菜单：

```text
New subcollection
Import paper here
Add selected papers here
Move selected papers here
Rename
Move collection...
Delete collection...
Suggest subtopics
Suggest papers for this collection
```

论文卡片右键菜单：

```text
Open Reader
Show details
Move to collection...
Add to collection...
Remove from current collection
Mark as reading
Archive
Discard
Run extraction
```

左侧顶部快捷入口：

```text
Collections    [+] [...]
```

- `+`：在当前选中 collection 下创建子 collection；未选中时创建顶层 collection。
- `...`：展开更多管理动作，例如 Expand all、Collapse all、Sort A-Z、Show empty collections、Validate library。

批量整理体验：

```text
3 papers selected
Drag them into a collection
or right-click a collection -> Add selected papers here
```

验收：

- 用户可以不打开任何隐藏表单完成创建、重命名、移动、删除 collection。
- 多选论文后可通过右键 collection 完成添加或移动。
- 删除 collection 有确认弹窗，并要求选择 paper policy。
- 每个操作都有 toast / notice 反馈。

#### 0.1.5 阶段五：论文拖拽到 Collection

分两步做，避免一次性把中间列表全部 React 化导致改动过大。

Phase 5A：先支持多选论文投递。

- 保留现有 Streamlit paper card。
- 中间列表继续用 `st.multiselect` 批量选择论文。
- 左侧 collection 右键菜单新增 `Add selected papers here` 和 `Move selected papers here`。
- 后端调用 `move_papers_to_node()` 或后续新增的 append/remove API。

Phase 5B：把 paper list 也纳入 React component。

- paper card 支持真实拖拽。
- 支持单篇拖拽到 collection。
- 支持多选后一起拖拽。
- drop 时左侧目标 collection 高亮。
- drop 后中间列表、左侧 count、右侧 detail panel 一起刷新。

拖拽规则：

- paper -> collection：默认移动 primary location。
- paper + modifier -> collection：增加 secondary location。
- selected papers -> collection：批量移动或批量添加。
- paper -> Work Queue：不允许，因为 Work Queue 不是存储结构。
- collection -> Work Queue：不允许。
- collection -> collection center：移动为子节点。
- collection -> collection top/bottom：同级 before / after 排序。
- technical generated node 默认不可作为真实存储目标，除非用户选择 Promote to collection。

验收：

- 5A 完成后，用户能通过多选 + 右键 collection 完成批量归档。
- 5B 完成后，用户能像文件管理器一样拖拽论文卡片到左侧 collection。

#### 0.1.6 阶段六：导入论文时直接选择 Collection

Paper Search 导入不应该只进入全局 inbox。用户在某个 collection 下搜索时，默认导入位置应该是当前 collection。

改动范围：

- `pages/7_Research.py` 的 Paper Search 导入表单。
- `src/nblane/core/research_papers.py` 的 import / search result handling。

导入流程：

```text
Paper Search
  -> Select papers
  -> Choose library location
  -> Optional: create child collection
  -> Optional: accept AI placement suggestions
  -> Import into Paper Library
  -> Open Reader or Stay in Library
```

导入选项：

- Import to current collection。
- Choose existing collection。
- Create new child collection。
- Ask AI to suggest collection。
- Import to Unsorted Inbox。
- Import as metadata only。
- Download open-access PDF。
- Upload local PDF。

规则：

- 在 collection 中打开 search 时，默认 `library_node_refs=[current_node]`。
- AI suggestion 只能作为 preview，用户确认后才写入。
- 导入后可以选择留在 Library 或打开 Reader。

验收：

- 从 `VLA / Memory` 下搜索并导入，论文默认出现在该 collection。
- 导入时可以创建 `VLA / Memory / Episodic Memory`。
- AI suggested location 显示 confidence 和 rationale，但不会静默移动论文。

#### 0.1.7 阶段七：模型 technical meta 抽取和自动分类

用户 Collections 解决“我为什么收藏它”；模型 technical meta 解决“论文客观上属于哪些技术维度”。两者必须并存。

建议结构：

```yaml
technical_meta:
  domains:
    - Embodied AI
    - Robotics
  tasks:
    - Robotic Manipulation
  methods:
    - Diffusion Policy
    - Vision-Language-Action
  datasets:
    - CALVIN
    - BridgeData
  benchmarks:
    - Multiview 3D Consistency
  modalities:
    - vision
    - language
    - action
  contribution_type:
    - dataset
    - method
  confidence:
    methods:
      Diffusion Policy: 0.86
  evidence:
    methods:
      Diffusion Policy:
        source: abstract
        quote_ref: segment:...
  reviewed:
    methods: false
```

任务：

- 增加单篇 `Extract technical meta`。
- 增加批量 technical meta extraction。
- 左侧生成虚拟 `Technical Taxonomy`：
  - Topics
  - Methods
  - Datasets
  - Benchmarks
- 支持用户确认 / 修正模型分类。
- 支持将某个 technical node `Promote to collection`。

验收：

- 模型分类有 confidence 和 evidence，不是黑盒 tag。
- 用户 collection 不会被模型静默改写。
- technical taxonomy 可以用于浏览，但默认不承担“真实归档位置”。

#### 0.1.8 阶段八：Graph Cluster / Library Map

当 technical meta、claims、citations、chunks 足够多后，新增图聚类视图，而不是继续堆属性分类。

关系模型：

```text
Paper -> belongs_to -> Topic
Paper -> proposes -> Method
Paper -> evaluates_on -> Dataset
Paper -> improves_over -> Baseline
Paper -> supports -> Claim
Paper -> cites -> Paper
Paper -> shares_method_with -> Paper
```

目标视图：

- Cluster View：按相似论文聚类。
- Method Lineage：方法演进线。
- Dataset / Benchmark Map：数据集和评测关系。
- Claim Network：论文观点、证据和引用关系。
- Citation Neighborhood：当前论文的上下游引用邻域。

规则：

- Graph Cluster 是探索视图，不替代左侧 collection tree。
- 每个 cluster 必须能解释“为什么这些论文在一起”。
- 用户可以把 cluster 保存为 collection。

验收：

- 从 collection 进入 graph view。
- 从单篇 paper 查看 related papers。
- cluster 可保存为 user collection。

#### 0.1.9 推荐里程碑

```text
M1：重排左侧信息架构
M2：补齐 tree backend 操作
M3：实现 Paper Library Tree custom component
M4：右键菜单 + 创建 / 重命名 / 删除 / 移动 collection
M5：多选论文 -> Add selected papers here
M6：导入论文时选择 / 创建 collection
M7：paper card 拖拽到 tree
M8：模型 technical meta 抽取
M9：Technical Taxonomy 自动视图
M10：Graph Cluster / Library Map
```

建议排期：

```text
Week 1
  - 重构左侧导航分组
  - 后端增加 rename / position / trash / purge / reorder
  - 单元测试覆盖树操作

Week 2
  - 做 paper_library_component
  - 接入右键菜单
  - 替换 Manage collections expander
  - 支持 collection create / rename / move / delete

Week 3
  - 批量选择论文后 Add selected papers here
  - 详情面板 collection picker 改造
  - Search 导入默认进入当前 collection

Week 4
  - React paper list 或轻量拖拽桥接
  - 支持 paper card 拖入 collection
  - Playwright 覆盖拖拽和右键菜单

Week 5+
  - technical meta 抽取
  - Methods / Topics / Datasets 自动视图
  - AI suggest location / split node / merge node

Later
  - graph clustering
  - method lineage
  - claim / citation network
```

#### 0.1.10 测试计划

单元测试：

- create node。
- rename node。
- move node into parent。
- position node before / after sibling。
- reject move under descendant。
- trash node with papers -> move to parent。
- trash node with papers -> move to unsorted。
- purge node removes source refs。
- move papers replace primary node。
- move papers append secondary node。
- `paper_rows()` node filter remains correct。

组件测试：

- context menu opens on right click。
- create collection emits correct event。
- rename emits correct event。
- drag node into another node emits move event。
- drag node before sibling emits reorder event。
- invalid drop shows no action。

E2E：

- right click Collections -> New collection -> appears in tree。
- right click node -> Rename -> title updates。
- drag child collection under another collection -> tree persists after reload。
- select papers -> right click node -> Add selected papers here -> paper count updates。
- open current collection -> imported paper appears there。

最终验收：

```text
1. 在左侧右键 Collections，创建 VLA。
2. 右键 VLA，创建 Memory。
3. 搜索论文时选择导入到 VLA / Memory。
4. 对已有论文多选，右键 VLA / Memory，选择 Add selected papers here。
5. 把 Memory 拖到另一个 topic 下。
6. 重命名 collection，不影响论文和 Reader。
7. 删除 collection 时选择把论文移动到父节点或 Unsorted。
8. 用 Work Queue 处理 PDF Missing / Needs Extraction，但不会把这些当成资料库结构。
```

产品成功标准：

> 用户管理论文时感觉自己在整理一个研究资料库，而不是在操作一堆状态筛选器。

## 1. 背景和目标

当前 Research Reader 已具备 PDF.js 阅读、highlight/comment、translation、review card、deep read、GROBID/PyMuPDF 抽取和 FastAPI sidecar 等基础能力，但阅读体验还没有形成一个稳定的“论文工作台”：

- 中文翻译尝试贴合 PDF rect 绝对定位，遇到表格、多栏和窄列时会被挤碎，基本不可读。
- compare/follow 主要按页同步，不够贴合真实段落阅读。
- 选区 toolbar 不能拖拽或固定，有时遮挡正文。
- 早期选区按钮文案偏内部对象名，用户不容易判断动作结果。
- highlight comment 点击后直接进入重编辑态，阅读 comment 太重，且点击页面空白不能自然退出。
- 侧边 panel 没有统一承载 comment、翻译结果和 review 信息。
- 多个分析入口分裂，用户心智上其实都是“分析这篇论文”。
- 论文载入时会等待抽取或 payload 构建，应该分阶段载入，先读 PDF，再后台补齐结构化文本、翻译和分析。
- GROBID 后台处理 PDF 很快，应该进入默认后台准备流程，而不是阻塞主阅读入口。

目标是把 Reader 升级为一个接近前沿论文阅读工具的可追溯工作台：PDF 立即打开，GROBID/PyMuPDF 后台准备结构化内容，翻译按段落流可读展示，批注和译文统一进入侧栏 review，AI 输出始终带 refs，用户可以快速判断论文价值，也可以继续深读和沉淀 claim/citation。

## 2. 设计原则

- **先阅读，后增强**：打开论文时先显示 PDF 和已有缓存，不因抽取、翻译、分析阻塞。
- **翻译可读优先于像素级复刻**：中文默认按 section/paragraph/table/caption 流式排版；原位 overlay 只作为 debug 能力。
- **所有 AI 输出可追溯**：翻译、解释、问答、分析都必须绑定 segment / annotation / chunk / citation refs。
- **批注先看再改**：highlight comment 默认进入阅读卡片，只有点击 Edit 才进入编辑态。
- **按钮说人话**：把内部对象名转成用户动作，比如 `Save quote`、`Cite this`。
- **长任务全部后台化**：全文翻译、GROBID 准备、Analyze Paper 走 task + SSE/polling，完成后刷新 payload。

## 3. 开发范围

优先主路径：

- `src/nblane/web_reader_api/templates/index.html`
- `src/nblane/web_reader_api/__init__.py`
- `src/nblane/core/research_papers.py`
- `src/nblane/core/reader_actions.py`
- `src/nblane/core/reader_tasks.py`
- `pages/7_Research.py`
- `tests/test_web_reader_api.py`
- `tests/test_research_papers.py`
- `tests/test_research_paper_reader_component.py`（仅事件契约和 retired runtime）

当前以 FastAPI sidecar Reader 为唯一主体验。`research_paper_reader_component` 的
Streamlit runtime 不再承载 PDF Reader，只保留 `events.py` 作为 action/state 契约来源。

## 4. Phase 1：分阶段载入和后台准备

### 4.1 行为

打开 Reader 时：

1. FastAPI `/payload` 只返回最小上下文：source、PDF URL、当前页附近 pages/segments/translations、已有 annotations、已有 analysis 摘要。
2. PDF range endpoint 立即可用，PDF.js 先渲染当前页。
3. 如果 pages/segments 缺失或 PDF fingerprint 变化，后台启动 `prepare_reader_artifacts`。
4. 顶部显示阶段状态：
   - `PDF ready`
   - `Extracting page text`
   - `Running GROBID`
   - `Structured text ready`
   - `Fallback text ready`
   - `Preparation failed`
5. 后台完成后刷新 payload，但不打断当前滚动位置。

### 4.2 后端任务

新增 reader task action：

```text
prepare_reader_artifacts
```

输入：

```json
{
  "source_id": "...",
  "page": 1,
  "prefer_grobid": true,
  "event_id": "..."
}
```

输出：

```json
{
  "summary": {
    "ready": true,
    "status": "ready|fallback|failed|missing_pdf",
    "pages": 12,
    "segments": 180,
    "structure_backend": "grobid|pymupdf_fallback|fallback",
    "warnings": []
  }
}
```

进度：

```json
{
  "phase": "extracting_pages|running_grobid|saving_segments|done",
  "label": "Running GROBID...",
  "current": 1,
  "total": 3,
  "saved": 0
}
```

### 4.3 关键实现

- `pages/7_Research.py` 上传 PDF / 下载 PDF 后，不再同步等待完整 structured extraction；只保存 PDF asset 和 page count，然后提示后台准备。
- `web_reader_api.__init__._payload_for_context()` 不再 `requested_pages=set(range(1, total_pages + 1))`，改成当前页窗口，例如 `{page-1,page,page+1}`。
- `reader_tasks.ALLOWED_READER_TASK_ACTIONS` 增加 `prepare_reader_artifacts`。
- `reader_actions` 处理 `prepare_reader_artifacts`，调用 `ensure_paper_reading_artifacts(..., prefer_grobid=True)` 并发送 progress callback。
- 前端 `loadReaderPayload({preserveView:true})` 完成后不重建 PDF 源，只刷新 annotations/translations/analysis/panel。

### 4.4 验收

- 打开一篇刚上传的 PDF，PDF 页面先显示，不等待 GROBID。
- 后台准备完成后，侧栏 segments/translation 可用。
- GROBID 失败时仍可阅读 PDF，并显示 fallback warning。
- `/payload` 响应不包含全论文所有 segments/translations。

## 5. Phase 2：翻译阅读流重构

### 5.1 行为

默认翻译视图改为“中文阅读流”，而不是按 PDF rect 覆盖：

- 右侧按 `section_path -> paragraph/order` 分组。
- 每个译文块显示 locator，例如 `p. 3 · § Method`。
- 状态显示 `translated / missing / stale / failed`。
- missing/stale 块显示操作：`Translate` 或 `Retry`。
- 点击译文块跳到 PDF 页并高亮对应 rect。
- 点击 PDF 中的 segment 或 annotation，右侧滚到对应译文。
- 表格不做窄框绝对定位；GROBID 能识别 table 时显示 table/caption card，否则显示页面文字块和页码。

### 5.2 前端改动

新增：

```js
function translationFlowHtml() {}
function translationUnitGroups() {}
function activeSegmentFromViewport() {}
function buildAnchorIndex() {}
function setActiveAnchor(anchorId, options = {}) {}
```

调整：

- `translationReaderHtml()` 默认调用 flow renderer。
- `renderTranslationPage()` 和 `.pr-translation-block.placed` 保留为 overlay mode，但不作为默认。
- `syncCompareScroll()` 从 page-level 同步改为 anchor-level 同步。
- `wireTranslationRows()` 支持 hover 高亮、click 跳转、retry missing/stale。

### 5.3 CSS

新增译文流样式：

```css
.pr-translation-flow {}
.pr-translation-section {}
.pr-translation-unit {}
.pr-translation-unit.active {}
.pr-translation-unit-status {}
.pr-translation-locator {}
.pr-translation-source-snippet {}
```

减少默认 overlay 强制定位：

- `.pr-translation-block.placed` 只在 `readerMode` 或 translation layout mode 为 `overlay` 时出现。

### 5.4 验收

- 截图中的表格翻译不再变成逐词竖排。
- 当前页中文译文能连续阅读。
- 点击译文能跳回 PDF 原文页。
- stale 翻译不会覆盖旧译文，且 UI 明确提示重新翻译。

## 6. Phase 3：可拖拽 toolbar 和清晰动作命名

### 6.1 新文案

选区 toolbar 按以下顺序显示：

```text
Translate
Highlight
Note
Explain
Ask with selection
Save quote
Cite this
```

含义：

- `Translate`：翻译当前选区。
- `Highlight`：只保存高亮。
- `Note`：高亮并添加 comment。
- `Explain`：解释当前选区的术语、难句、公式上下文。
- `Ask with selection`：把选区作为上下文向整篇论文提问。
- `Save quote`：保存为可复用 research chunk。
- `Cite this`：创建 claim/citation 候选。

保留后端 action 名：

- `create_chunk_from_selection`
- `create_citation`
- `explain_selection`
- `ask_paper`

只改 UI 文案和前端 action 分组，不改存储语义。

### 6.2 拖拽行为

Toolbar 支持：

- 拖拽移动。
- pin/unpin。
- close。
- reset position。
- 底部 dock fallback。

local/session state 增加：

```json
{
  "dock_position": {"x": 320, "y": 120},
  "dock_pinned": true,
  "dock_layout": "floating|bottom|selection"
}
```

### 6.3 验收

- toolbar 可以被鼠标拖到不遮挡阅读的位置。
- 刷新当前 reader 后位置保持。
- Escape 或 close 能关闭 toolbar。
- 内部对象名不再作为裸按钮文案出现。

## 7. Phase 4：Comment 阅读态 / 编辑态拆分

### 7.1 行为

点击 highlight：

1. 默认打开 comment 阅读卡片。
2. 展示 selected text、note/comment、tags、locator、chunk refs。
3. 操作按钮：`Edit`、`Save quote`、`Cite this`、`Delete`。
4. 点击空白处关闭。
5. 点击 `Edit` 后进入 textarea 编辑态。
6. 编辑态 Save / Cancel；未修改时点击空白可关闭，有修改时保留确认操作。

### 7.2 前端状态

替换当前：

```js
let annotationPopover = { open, annotation, pending, x, y }
```

为：

```js
let annotationPopover = {
  open: false,
  mode: "read", // read | edit
  annotation: null,
  pending: false,
  dirty: false,
  x: 16,
  y: 16
}
```

### 7.3 侧栏 Notes

Notes tab 显示：

- comment 优先，其次 selected text。
- page locator。
- tag/chunk/citation 状态。
- 当前页过滤和 all/filter toggle。

### 7.4 验收

- 点击有 comment 的 highlight 可以轻量阅读。
- 点击页面空白可以关闭阅读卡片。
- 只有点 Edit 才进入编辑框。
- 新建 note 仍然可以从选区 toolbar 进入编辑态。

## 8. Phase 5：统一侧栏 panel

Tabs 调整为：

```text
Notes | Translation | Review | Claims
```

### 8.1 Notes

- annotations 列表。
- comment 摘要。
- jump/edit/delete。
- current page/all filter。

### 8.2 Translation

- 当前页或当前 section 的翻译流。
- missing/stale 统计。
- translate visible / retry stale。
- 点击译文跳 PDF。

### 8.3 Review

- TL;DR。
- key points。
- method / experiments。
- limitations。
- score grid。
- project relevance。
- reading plan。
- cited refs。

### 8.4 Claims

- chunks。
- citation candidates。
- research claims。
- 从条目跳回 PDF locator。

### 8.5 验收

- 用户可以在侧栏统一阅读 comment 和译文。
- Analyze Paper 输出能直接显示在 Review tab。
- Claims tab 能看到从选区创建的 quote/citation。

## 9. Phase 6：Analyze Paper 合并（已完成）

### 9.1 新按钮

顶部已去掉旧的并列分析入口：

```text
Review
Codex long read
```

改为一个：

```text
Analyze Paper
```

### 9.2 后端 action

新增：

```text
analyze_paper
```

兼容旧 action：

- `generate_review_card`
- `codex_deep_read`

旧 action 保留 API 兼容，但 UI 不作为主按钮展示。

### 9.3 输出结构

保存到 `research/analysis/<source>.yaml`：

```yaml
schema_version: "1.0"
source_id: ...
updated: ...
tldr: ...
key_points: []
method: []
experiments: []
limitations: []
usefulness: ...
scores:
  novelty: 0
  technical_depth: 0
  evidence_quality: 0
  reproducibility: 0
  relevance: 0
  overall: 0
score_rationale: []
project_relevance: []
reading_plan: []
open_questions: []
cited_segment_refs: []
cited_chunk_refs: []
cited_annotation_refs: []
warnings: []
```

### 9.4 AI 行为

- 默认先用 direct LLM 生成 review card。
- 如果 Codex local backend 可用，可附加更长的 deep reading findings。
- 结果合并后一次写入 analysis。
- 所有 substantive claim 和评分理由必须带 refs；无 refs 时降级为 warning。

### 9.5 验收

- 一次点击生成关键信息和评分。
- Review tab 不需要用户理解旧分析入口之间的差别。
- 旧 API 测试仍通过。

## 10. 测试清单

### 10.1 单元测试

- `build_reader_payload` 只返回上下文窗口，不返回全论文。
- GROBID 可用时生成 structured segments 和 refs。
- GROBID 不可用时 fallback，不阻塞 PDF。
- stale translation 不覆盖现有译文。
- `analyze_paper` 输出包含 scores 和 cited refs。

### 10.2 API 测试

- `/tasks` 支持 `prepare_reader_artifacts`。
- `/tasks` 支持 `analyze_paper`。
- SSE snapshot 包含 phase/progress/refresh。
- task done 后前端可刷新 payload。

### 10.3 前端 contract 测试

- HTML 包含 draggable dock 状态。
- HTML 包含 annotation read/edit mode。
- HTML 包含 Translation panel tab。
- HTML 包含 Analyze Paper 按钮。
- HTML 不再暴露早期内部对象名式按钮文案。

### 10.4 手动验收

用一篇包含双栏、表格、图和 references 的 PDF：

1. 首次打开 PDF 立即显示第一页。
2. 后台 GROBID 状态可见。
3. 翻译当前页后中文可连续阅读。
4. 全文翻译不把表格翻译挤成竖排碎片。
5. 点击中文译文跳回 PDF 页。
6. 点击 highlight 先看 comment，点空白关闭。
7. toolbar 可拖拽移动。
8. Analyze Paper 一次生成 TL;DR、评分和阅读建议。
9. 新建 quote/citation 后可在 Claims tab 看到。

## 11. 风险和注意事项

- 静态组件 runtime 已退出主路径；后续 Reader 行为应继续收敛到 FastAPI sidecar 模板和 core handler。
- GROBID coordinates 解析可能不完整，不能把 overlay 作为普通翻译阅读方式。
- 全文翻译和 Analyze Paper 可能很慢，必须走后台任务，不能回到同步阻塞。
- AI activity 不能记录完整 PDF、全文翻译或完整 prompt，只记录摘要和 refs。
- PyMuPDF 许可边界仍需在部署文档中保持可见。
- `reader_actions.py` 和 `pages/7_Research.py` 有重复 action 处理逻辑，后续应逐步收敛到 core handler，减少行为分叉。

## 12. 参考方向

- Zotero PDF Reader：批注侧栏、note 和 PDF locator 的成熟模式。
  <https://www.zotero.org/support/pdf_reader>
- Semantic Reader：TOC、citation cards、skimming/highlight 等阅读增强。
  <https://www.semanticscholar.org/product/semantic-reader>
- Semantic Scholar TLDR：快速判断论文是否值得深读。
  <https://www.semanticscholar.org/product/tldr>
- GROBID：学术 PDF 到 TEI、full text、references 和 coordinates。
  <https://grobid.readthedocs.io/en/latest/Grobid-service/>
  <https://grobid.readthedocs.io/en/latest/Coordinates-in-PDF/>
