# 未完成开发

> 生成日期：2026-05-22  
> 对照来源：`开发.md`、当前代码、测试和 `docs/zh/guides/web-ui.md`。  
> 口径：只记录从开发计划看仍未达到验收、或只完成了局部骨架的事项；已经有明确代码、测试、文档支撑的事项不重复列入。

## 当前结论

当前主要未完成不在 PDF Reader 基础能力，也不在 Paper Library 8502 主路径，而集中在：

- Research Workspace 四个 tab 的“可操作工作台”体验仍有收尾，主要是更多 8502 高频 surface 和输出候选类型。
- Paper Library 已迁到 8502，Paper Search / PDF candidate import 已进入 8502；虚拟列表、键盘工作流、Manual URL / Upload PDF 和 split Reader 仍未进入同一个 8502 canvas。
- Connector Inbox 已有 dry-run candidate cards、selected import、provider/run summary、专用 manual import 面板和 metadata-only target，但人类化错误/限流状态与 8502 迁移还没完成。
- Synthesis / Export 已补 scope selector、manifest gate、多篇 reading note pack、draft coverage review、RIS / CSL JSON、blocker 修复入口，以及 blog / project update / resume bullet 三类 Output Candidates。
- Technical Taxonomy 目前还是空壳入口，Graph Cluster / Library Map 尚未开始。

## 已从未完成项剔除

这些条目在 `开发.md` 中曾列为后续或未完成，但当前已经落地，不再作为未完成开发跟踪：

- **Paper Library runtime feature flag**：`NBLANE_PAPER_LIBRARY_RUNTIME` 已支持 `streamlit_component`、`fastapi_link`、`fastapi_iframe`，默认 `fastapi_link`；`docs/zh/guides/web-ui.md` 已更新说明，`tests/test_research_papers.py` 有归一化测试。
- **8502 不可用时 fallback 提示**：`pages/7_Research.py` 会探测 8502 Paper Library，失败时提示并显示 Streamlit fallback。
- **Paper Library 8502 主体验**：standalone `/paper-library`、API、tree/list/detail 同 canvas、Danger Zone、Playwright E2E 和截图验收均已完成主路径。
- **Reader 基础六阶段主路径**：`prepare_reader_artifacts`、翻译 flow、可拖拽 toolbar、comment read/edit、侧栏 `Notes / Translation / Review / Claims`、`Analyze Paper` 已有模板和测试覆盖。
- **Synthesis / Export 的基础 scope selector**：collection、goal、source status、claim status、tag、manual sources 已接入 `build_research_export_payload()`；不再把“scope selector 从零开始”列为未完成。
- **多篇 reading note pack**：单篇 note 仍走 paper note，多篇 source 已能保存 export pack 并旁挂 manifest。
- **Connector manual import 面板**：pasted URLs、CSV、JSON list 已走专用 preview、dedupe、selected import 和 collection target 流程。
- **Connector 导入后基础 next actions**：Advanced Connectors 已给 paper imports 8502 Paper Library 入口；本地 PDF asset 就绪时给 Reader / extraction。
- **Paper Search 导入时选择 Collection**：Streamlit 侧 `Find and import papers` 已支持默认当前 collection、选择 location、创建 child collection、导入后聚焦目标 collection。
- **8502 Paper Search PDF 导入闭环**：8502 standalone Paper Library 已有 Codex / provider PDF search、candidate cards、collection / status / visibility target、selected import 和导入后聚焦；Manual URL / Upload PDF 仍列在后续收尾。
- **Claims Review 卡片级审阅闭环**：claim card 已支持 patch text / type / confidence / warnings / human note、替换 chunk/citation links、从 chunk 建 citation、Request citation、dismiss rationale、ready claim 批量 quote check / mark ready / promote preview、duplicate claim 合并提示与 citation source locator。
- **Synthesis / Export 基础写作出口补强**：空 synthesis draft 已生成 outline、argument map、coverage 和 missing citation warnings；draft coverage review、blocker repair entry、可扫描 manifest provenance tabs、RIS 与 CSL JSON citation export 已落地。
- **Output Candidates 扩展**：Synthesis draft 已能生成 blog draft、project update 和 resume bullet candidate；三类候选都带 `related_sources` / `related_research_claims` / `related_citations`，并复用 export manifest gate 检查 private source、unpromoted research claim 和 broken citation。
- **Connector metadata-only target**：Connector candidate import 已能明确选择 Source Inbox、collection 或 metadata only；metadata-only 来源会记录 import target metadata。
- **Overview target contract 与 8502 Paper Library 深链**：`build_research_overview_payload()` 已输出 `pipeline`、`work_queues`、`recent_work`、`safety` 和结构化 `target`；8503 Overview 队列已生成真实 8502 deep link；8502 `/paper-library` 已消费 `focus/action/return_to` 并在 detail pane 高亮目标区块和建议动作；已补单测和 Playwright 闭环。

## P0：下一轮最该补齐

### 3. Synthesis / Export 仍缺写作出口的高级能力

现状判断：

- Export manifest、private source / unpromoted claim / broken citation blockers、保存 `.manifest.yaml` 已落地。
- Blog candidate readiness gate、blocker repair entry 和可扫描 provenance tabs 已落地。
- Scope selector 已落地。
- Reading note export 已覆盖单篇 source 和多篇 reading note pack。
- 空 synthesis draft 已生成 outline、argument map、coverage、missing citation warnings，页面已有 draft coverage review。
- Citation export 已支持 Markdown、BibTeX、RIS 和 CSL JSON。

未完成任务：

- 将 blocker 修复入口继续做成跨 surface 的精确定位，而不是 Claim tab 的队列提示。

验收：

- 用户能按 collection / goal 生成一组论文的 reading note pack。
- Draft 能继续把 review payload 交给后续 8502 输出 surface，而不是只留在 Streamlit。
- blog / project update / resume bullet candidate 与同一 provenance gate 相连。

### 4. Advanced Connectors 仍缺完整 Connector Inbox

现状判断：

- `discover_connector_items()` dry-run 不写 sources，candidate cards、duplicate reason、selected import、collection target 已落地。
- pasted URLs、CSV、JSON list 已有 manual import 面板和 selected import；connector config 和 run now fallback 仍占较多视觉。

未完成任务：

- Provider cards / run history 继续 polish：补人类化 error、rate limit 状态、dedupe history，并迁到更合适的 8502 高频 surface。
- 导入成功后的 next actions 继续补 collection / Source Inbox 级批量动作和 extraction task progress。
- 在现有 Source Inbox / collection / metadata only 基础上补“当前 collection”快捷目标。

验收：

- 用户不看 YAML 也能完成发现、去重、选择目标、导入。
- X / Xiaohongshu 没 token 时仍有清晰手动导入路径。
- 导入后能直接进入下一步阅读或整理。

## P1：8502 深化和 Paper Library 规模化

### 5. 8502 高交互迁移还只覆盖 Paper Library

已完成的是 Paper Library standalone。以下计划中的 8502 页面仍未迁移：

- Claim review board。
- Citation inspector。
- Connector discovery candidate cards。
- Export manifest preview。

验收：

- 高频审阅和导出路径可以不依赖 Streamlit rerun。
- 8502 和 8503 共用现有 core payload / event handler，不新增事实源。

### 6. Paper Search 的 8502 导入闭环仍有收尾

现状判断：

- Streamlit 侧 `Find and import papers` 已可用，并已放回 8503 Paper Library 顶部，作为 fallback 导入入口。
- 搜索结果已从横向大表格改为粗读候选卡，支持 abstract、AI overview、why relevant、讲解链接、warnings 和导入勾选；表格和 YAML 仅保留为折叠调试信息。
- 8502 Paper Library React component 已有 standalone DiscoveryPanel，支持 Codex / provider PDF search、candidate cards、collection / status / visibility target、selected import 和导入后聚焦。

未完成任务：

- 把 Manual URL / Upload PDF 迁入 8502 Paper Library。
- Codex search / provider search 的 warnings、duplicate risk、PDF strategy、prepare reader 进入同一个 React flow。

验收：

- 不打开 8503，也能在 8502 完成搜索、预览、手工 URL / PDF 导入、继续阅读。

### 7. Paper Library 大列表和键盘操作

现状判断：

- 8502 paper list 是普通 `papers.map(...)` 渲染。
- context menu 只处理 Escape 关闭；没有完整方向键、范围选择、PageUp / PageDown、Home / End。

未完成任务：

- Virtualized paper list。
- 方向键移动选中 paper。
- Shift 多选范围。
- 批量选择后键盘或菜单移动到 collection。
- 结构化 query grammar：`tag:robotics status:reading has:pdf claims:0 after:2026-05-01`。
- Saved filters：用户保存自定义筛选。

验收：

- 大量论文时仍能顺滑滚动和搜索。
- 用户可以用键盘完成常见浏览和批量整理。

### 8. Reader 与 Library 深度联动

现状判断：

- Library detail pane 已显示 PDF、last page、segments、annotations、claims、citations 等基础指标。
- Reader 从 Library 打开已经是主路径。
- split reader / inline preview 尚未完成。

未完成任务：

- `Open in split reader`：同一 8502 工作台内打开 Reader iframe。
- Reader 保存 annotation / translation / chunk / claim 后，Library detail 局部刷新计数。
- detail pane 补充 visible pages、active tab、target language、translation coverage、segment coverage。
- Work Queue 中 `Stale Translation`、`Needs Extraction`、`Claims Need Review` 与 Reader action 结果联动刷新。
- 批量 GROBID、批量 extraction、批量 claim generation 的 task queue 和进度反馈。

验收：

- 在 Library 中打开 Reader，关闭后仍回到同一 selected paper 和 collection。
- Reader 写入后 Library detail 和 queue count 不需要手动全量刷新才能看到变化。

## P2：研究资料库智能层

### 9. Technical Taxonomy 仍是空壳

现状判断：

- Paper Library payload 里已有 `Technical Taxonomy` section，但 topics / methods / datasets / benchmarks count 为 0。
- 还没有 `technical_meta` 抽取、review、修正和 promote to collection。

未完成任务：

- 单篇 `Extract technical meta`。
- 批量 technical meta extraction。
- technical meta 带 confidence 和 evidence refs。
- 虚拟 taxonomy 节点按 topics / methods / datasets / benchmarks 展示真实计数。
- 用户确认 / 修正模型分类。
- `Promote to collection`。
- AI suggested locations 从 preview 变成可确认操作。

验收：

- 模型分类不静默覆盖用户 collection。
- 每个 technical meta 都能解释来源和置信度。

### 10. Graph Cluster / Library Map 尚未开始

未完成任务：

- Cluster View。
- Method Lineage。
- Dataset / Benchmark Map。
- Claim Network。
- Citation Neighborhood。
- cluster 保存为 user collection。

验收：

- 从 collection 或单篇 paper 进入 graph view。
- 每个 cluster 都说明为什么这些论文在一起。
- graph view 只是探索视图，不替代 collection tree。

## 建议下一步顺序

1. **Overview 可点击队列**：先让 Command Center 真正能带用户进入下一步。
2. **Connector provider/run polish**：继续减少 YAML 配置感，补自动化状态和 dedupe / rate-limit 边界。
3. **8502 Manual URL / Upload PDF import wizard**：让 8502 不依赖 8503 完成论文引入。
4. **8502 Claims / Export / Connector 三个高交互页面**：逐步减少 Streamlit rerun 对高频路径的影响。
5. **Virtualized list + keyboard workflow**：解决论文规模变大后的可用性。
6. **Technical Taxonomy**：在资料库结构稳定后再做模型分类和图谱。

## 当前可视为完成的产品边界

当前系统已经可以支撑：

- 8502 Paper Library 作为主入口管理论文。
- 从 Library 打开 FastAPI sidecar Reader。
- Reader 中进行 PDF 阅读、翻译、批注、Analyze Paper、chunk / claim / citation 基础沉淀。
- Claims & Citations 中做基础审阅和 promotion。
- Synthesis / Export 中做 manifest-gated citation export，以及 blog / project update / resume bullet candidate preview。
- Connector dry-run 预览并选择性导入 source。

剩余开发的目标不是再补一个“能跑的骨架”，而是把这些已经能跑的模块打磨成连续、可导航、可批量处理、可回溯的研究工作台。
