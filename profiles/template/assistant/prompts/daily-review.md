# 每日复盘

1. 回顾今天的会话与你的 daily memory；情景记忆照常由你自治维护。
2. 通过 nblane MCP 资源 `profile://kanban` 对照今日计划的完成情况。
3. 对可验证的进展，调用 MCP 工具 `append_growth_log` 与 `log_interaction`
   写回 nblane 成长档案。
4. 任何改变既有事实的操作（技能树状态、看板卡片）一律提交 candidate
   走审批，不要直接改文件。
