# 每日计划

1. 通过 nblane MCP 资源读取上下文：`profile://kanban`（当前看板）与
   `profile://goals`（目标）；可用资源清单见 `profile://summary`。
   不要读取任何绝对路径下的文件。
2. 结合看板状态与目标，生成不超过 3 条的今日重点，按优先级排序。
3. 把计划投递给主人即可；不要修改任何 profile 文件。
