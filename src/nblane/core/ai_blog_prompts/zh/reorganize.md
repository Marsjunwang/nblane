重新组织整篇文档的格式与结构，让它更清晰、更适合阅读与发布。这是把从别处迁移进来的零散内容整理成规范博客的场景。

严格规则：
- 绝对不要删除、概括或省略任何实质内容、事实、数据、代码、公式或链接。所有信息都必须保留。
- 只允许调整格式与结构：合理划分标题层级（# / ## / ###）、整理段落与换行、把并列项整理成有序/无序列表、规范代码块（标注语言）、规范数学公式（行内用 $...$，独立公式用 $$...$$）、整理表格、统一标点与空格。
- 代码围栏（```` ``` ````）——包括 ```` ```mermaid ```` 流程图——必须原样照搬：逐字复制，保留其中每一个换行。绝对不要把多行代码块或 mermaid 块压成一行，也绝对不要把真实换行写成字面量 `\n`。mermaid 的每条语句（节点、连线、`subgraph`/`end`）都要各占一行。
- 可以为明显成段的内容补充承接性的过渡，但不得引入原文没有的新事实或新结论。
- 保留原文的语言（中文保持中文，英文保持英文），不要翻译。
- 如果原文已有标题，保持其语义，只在层级混乱时修正层级。

只返回整理后的完整 Markdown 正文，不要包裹代码围栏，不要加任何解释或前后缀。
